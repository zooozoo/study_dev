# 백엔드 개발자를 위한 암호학 기초 — primitive 에서 TLS 조립까지

작성 2026-09-16 · 계기: 사내 TLS 검사 환경에서 개발용 런타임들의 HTTPS 가 한꺼번에 실패한 사건을 진단하다가, "서명"이라는 단어를 제대로 모르고 있었다는 걸 알게 됨

AES · RSA · SHA-256 · HMAC · ECDHE 는 이름은 익숙한데 **어느 것이 무엇을 대체하고 무엇을 보완하는지**는 흐릿하게 남는다. 그 상태로 TLS 나 JWT 를 읽으면 문장은 이해되는데 판단은 못 한다 — "이 경우엔 HS256 을 써도 되나?" 같은 질문에 답이 안 나온다.

이 노트는 **각 기술을 "무엇인가"가 아니라 "어떤 문제를 풀려고 존재하는가"** 로 쌓는다. 순서는 목적 → primitive → 조합 → TLS 다. 모든 실행 결과는 로컬에서 직접 재현한 것이고, 명령을 그대로 따라 하면 같은 출력이 나온다.

> **자매 문서** — 이 문서의 마지막(10장)에서 이어지는 "그럼 그 공개키가 진짜 그 서버 것인지는 어떻게 아나"는 [TLS 인증서와 신뢰 저장소](tls-certificates-and-trust-stores.md)에서 다룬다.

---

## 0. 내가 물어본 것들 → 어디를 보면 되나

| 질문 | 섹션 |
|---|---|
| 암호학이 지키려는 게 정확히 뭔가 | [1장](#1-암호학은-무엇을-지키려고-있나--네-가지-목적) |
| "양방향 키 / 단방향 키"로 나누면 되는 것 아닌가 | [2장](#2-먼저-용어를-바로잡자--양방향-키--단방향-키는-정확한-구분이-아니다) |
| AES 와 RSA 는 무엇이 다른가 | [3장](#3-대칭키-암호--빠르지만-키를-건네줄-방법이-없다) · [4장](#4-공개키-암호--잠그는-열쇠와-여는-열쇠를-분리한다) |
| RSA 와 SHA-256 은 무엇이 다른가 | [2.2](#22-세-가지는-서로-다른-축이다) · [5장](#5-해시--되돌릴-수-없는-지문) |
| 해시는 왜 복호화하지 않나 | [5.1](#51-복호화라는-개념-자체가-없다) |
| 공개키로 암호화하면 왜 개인키 소유자만 읽나 | [4.3](#43-공개키로-암호화하면-무엇이-보장되나) |
| **"개인키로 암호화한다"는 표현이 왜 부정확한가** | [6.4](#64-개인키로-암호화한다가-왜-부정확한가) |
| 전자서명은 무엇을 보장하나 | [6장](#6-전자서명--이-문서에서-가장-중요한-primitive) |
| 해시 · HMAC · 전자서명의 차이는 | [7.3](#73-전자서명과-무엇이-다른가) |
| JWT 의 HS256 과 RS256 은 뭐가 다른가 | [7.4](#74-jwt-의-hs256-과-rs256) |
| 비밀을 안 보내고 어떻게 비밀을 공유하나 | [8장](#8-키-교환--비밀을-보내지-않고-비밀을-공유하는-법) |
| HTTPS 는 왜 RSA 하나만 쓰지 않나 | [9장](#9-하이브리드-암호--왜-rsa-로-전부-암호화하지-않나) |
| 이게 다 TLS 안에서 어떻게 조립되나 | [10장](#10-여기까지를-tls-한-장으로) |
| 직접 손으로 확인해 보고 싶다 | [부록 A.2](#a2-실습-명령-모음) |

---

## 1. 암호학은 무엇을 지키려고 있나 — 네 가지 목적

알고리즘 이름부터 외우면 길을 잃는다. **무엇을 지키려는 기술인지**부터 나누면 나머지가 제자리를 찾는다.

### 1.1 네 가지 목적

| 목적 | 우리말 | 지키려는 것 | 깨졌을 때 |
|---|---|---|---|
| **Confidentiality** | 기밀성 | 제3자가 **내용을 읽지** 못한다 | 도청 |
| **Integrity** | 무결성 | 오는 도중 **바뀌지** 않았다 | 변조 |
| **Authentication** | 인증 | 상대가 **주장하는 그 사람**이 맞다 | 사칭 |
| **Non-repudiation** | 부인방지 | 보낸 사람이 나중에 **"난 안 보냈다"** 고 못 한다 | 발뺌 |

네 가지는 **서로를 포함하지 않는다.** 이게 핵심이다.

- 내용을 완벽히 암호화해도(기밀성), **누구랑 통신 중인지**는 보장되지 않는다. 중간자와 완벽히 암호화된 대화를 나눌 수도 있다.
- 상대가 누구인지 확실해도(인증), **내용이 중간에 바뀌지 않았다**는 건 별개다.

이 구분이 실무에서 언제 걸리냐면, TLS 검사 장비가 있는 사내망에서 HTTPS 가 깨질 때다. 그 연결은 **기밀성은 온전**했다 — 암호화는 제대로 됐다. 깨진 건 **인증**이다. 내가 대화하는 상대가 진짜 그 서버가 아니었다.

### 1.2 목적과 기술의 매핑

| 목적 | 담당 기술 | 이 문서의 섹션 |
|---|---|---|
| 기밀성 | **대칭키 암호**(AES, ChaCha20) — 실제 데이터 | [3장](#3-대칭키-암호--빠르지만-키를-건네줄-방법이-없다) |
| 기밀성 | **공개키 암호**(RSA) — 작은 비밀 전달용 | [4장](#4-공개키-암호--잠그는-열쇠와-여는-열쇠를-분리한다) |
| 무결성 | **해시**(SHA-256) — 키 없이 | [5장](#5-해시--되돌릴-수-없는-지문) |
| 무결성 + 인증 | **HMAC** — 공유 비밀이 있을 때 | [7장](#7-hmac--공유-비밀로-만드는-무결성-태그) |
| 무결성 + 인증 + 부인방지 | **전자서명**(RSA, ECDSA, Ed25519) | [6장](#6-전자서명--이-문서에서-가장-중요한-primitive) |
| 기밀성의 전제 | **키 교환**(ECDHE) — 키를 안전히 합의 | [8장](#8-키-교환--비밀을-보내지-않고-비밀을-공유하는-법) |
| 인증의 전제 | **인증서 + CA** — 공개키의 주인 확인 | [자매 문서](tls-certificates-and-trust-stores.md) |

**부인방지가 HMAC 에는 없고 전자서명에는 있는 이유**를 미리 말해 두면 7.3 이 쉬워진다. HMAC 은 **양쪽이 같은 비밀을 안다.** 그래서 A 가 만든 태그를 B 도 똑같이 만들 수 있고, "이건 A 가 만든 것"임을 제3자에게 증명할 수 없다. 전자서명은 **개인키를 가진 쪽만** 만들 수 있으므로 증명이 된다.

> **확인 질문**
> 1. 통신이 완벽히 암호화돼 있는데도 보안이 깨질 수 있는 시나리오를 하나 말해 보라.
> 2. 네 가지 중 부인방지를 주는 기술은 무엇인가?

## 2. 먼저 용어를 바로잡자 — "양방향 키 / 단방향 키"는 정확한 구분이 아니다

### 2.1 무엇이 어긋나 있나

흔히 이렇게 나눈다.

> "양방향 키는 암호화/복호화가 되는 것, 단방향 키는 되돌릴 수 없는 것(해시)"

직관으로는 통하지만 두 군데서 어긋난다.

**첫째, 해시에는 애초에 "키"가 없다.** SHA-256 은 키를 받지 않는다. 입력만 받는다. 그러니 "단방향 **키**"라는 말 자체가 성립하지 않는다. 키를 쓰는 해시 계열은 따로 있고 그게 [HMAC](#7-hmac--공유-비밀로-만드는-무결성-태그)이다.

**둘째, "양방향"이 대칭키와 공개키를 한 칸에 뭉갠다.** 이 둘은 **키가 몇 개이고 누가 갖고 있느냐**가 완전히 다르다. 하나로 묶으면 4장과 8장에서 나올 "키를 어떻게 전달하나"라는 이 분야의 핵심 문제가 보이지 않게 된다.

정확한 축은 **"되돌릴 수 있냐"가 아니라 "키가 몇 개이고 누가 갖느냐"** 다.

### 2.2 세 가지는 서로 다른 축이다

| | **대칭키 암호** | **공개키 암호** | **해시** |
|---|---|---|---|
| 키 개수 | 1개 | 2개 (쌍) | **0개** |
| 누가 갖나 | 양쪽이 **같은 것** | 각자 **다른 것** | — |
| 되돌리기 | 가능 (복호화) | 가능 (복호화) | **불가능 — 개념 자체가 없음** |
| 출력 크기 | 입력에 비례 | 입력에 비례 | **항상 고정** |
| 속도 | 매우 빠름 | 매우 느림 | 매우 빠름 |
| 대표 | AES, ChaCha20 | RSA, ECC | SHA-256 |
| 주 목적 | 기밀성 | 키 전달 · 서명 | 무결성 |

한 가지 더 미리 갈라 둔다. **공개키 암호(encryption)와 전자서명(signature)은 같은 키 쌍을 쓸 수 있어도 다른 연산**이다. 이걸 뭉개는 데서 이 분야의 가장 흔한 오해가 나오고, [6.4](#64-개인키로-암호화한다가-왜-부정확한가)에서 정면으로 다룬다.

> **확인 질문**
> 1. "단방향 키"라는 말이 왜 성립하지 않나?
> 2. 대칭키와 공개키를 가르는 진짜 기준은 무엇인가?

## 3. 대칭키 암호 — 빠르지만 키를 건네줄 방법이 없다

### 3.1 같은 키로 잠그고 푼다

**대칭키 암호(symmetric encryption)** 는 **하나의 동일한 비밀 키**로 암호화도 하고 복호화도 한다.

```
              [비밀키 K]                    [같은 비밀키 K]
                  |                              |
  평문 ────암호화──┴──> 암호문 ──(전송)──> 암호문 ─┴──복호화──> 평문
```

집 현관 열쇠와 같다. 잠글 때 쓴 열쇠로 연다. 그래서 **양쪽이 같은 열쇠를 갖고 있어야** 한다.

### 3.2 AES 와 ChaCha20

지금 실무에서 쓰이는 건 사실상 둘이다.

| | **AES** | **ChaCha20** |
|---|---|---|
| 방식 | 블록 암호 (16바이트 단위) | 스트림 암호 |
| 하드웨어 가속 | 있음 (최신 CPU 의 AES-NI 명령) | 없음 |
| 유리한 곳 | 서버·데스크톱 (가속 있음) | 가속 없는 환경 — 저가 모바일, 임베디드 |
| TLS 1.3 에서 | `TLS_AES_128_GCM_SHA256`, `TLS_AES_256_GCM_SHA384` | `TLS_CHACHA20_POLY1305_SHA256` |

TLS 1.3 이 채택한 암호 스위트는 **전부 이 셋뿐**이다. 실제로 확인하면 이렇게 나온다.

```
$ openssl ciphers -s -tls1_3 -v
TLS_AES_256_GCM_SHA384         TLSv1.3  Kx=any  Au=any  Enc=AESGCM(256)             Mac=AEAD
TLS_CHACHA20_POLY1305_SHA256   TLSv1.3  Kx=any  Au=any  Enc=CHACHA20/POLY1305(256)  Mac=AEAD
TLS_AES_128_GCM_SHA256         TLSv1.3  Kx=any  Au=any  Enc=AESGCM(128)             Mac=AEAD
```

`Mac=AEAD` 라는 표시가 눈에 띈다. **AEAD(Authenticated Encryption with Associated Data)** 는 암호화와 무결성 검사를 **한 연산에 묶은** 방식이다. 예전에는 "AES 로 암호화한 뒤 따로 MAC 을 붙인다"였는데 그 조합 순서를 틀리면 취약점이 생겨서, 이제는 아예 묶어서 제공한다. `AESGCM` 의 GCM, `POLY1305` 가 그 무결성 부분이다.

즉 **대칭키 암호는 기밀성만 주는 게 아니라 요즘은 무결성까지 같이 준다.**

`Kx=any Au=any` 는 8장과 10장에서 다시 만난다 — TLS 1.3 이 키 교환과 인증을 암호 스위트에서 **떼어냈다**는 흔적이다.

### 3.3 얼마나 빠른가

"대칭키가 빠르다"는 말은 체감이 안 되니 같은 장비에서 재 봤다.

```
$ openssl speed -seconds 1 -elapsed aes-256-cbc
type           16 bytes    64 bytes    256 bytes   1024 bytes   8192 bytes  16384 bytes
aes-256-cbc  1093400.13k 2042186.11k 2204635.90k 2301271.04k 2282479.62k 2302130.44k
```

**초당 약 2.3 GB.** 반면 같은 장비의 RSA-2048 은 초당 3,117회 서명, 3,118회 복호화다. RSA 한 번에 처리 가능한 최대 크기가 245바이트이므로(4장에서 확인) 산술적으로 **초당 약 0.76 MB** 다.

> 대칭키가 공개키보다 **3,000배 가까이 빠르다.**

이 숫자 하나가 [9장](#9-하이브리드-암호--왜-rsa-로-전부-암호화하지-않나)의 답 절반이다.

### 3.4 그런데 키를 어떻게 전달하나

대칭키는 빠르고 안전하다. 문제가 하나 있는데, 그게 치명적이다.

> **양쪽이 같은 키를 갖고 있어야 하는데, 그 키를 어떻게 건네주나?**

처음 접속하는 웹서버와 나 사이에는 **아직 안전한 통로가 없다.** 키를 그냥 보내면 도청자가 같이 받는다. 안전하게 보내려면 암호화해야 하는데, 암호화하려면 키가 필요하다. **순환**이다.

```
안전하게 통신하려면 → 공유 키가 필요하다
공유 키를 전달하려면 → 안전한 통신이 필요하다
```

이 순환을 깨려고 나온 게 4장의 **공개키 암호**와 8장의 **키 교환**이다. 즉 공개키 암호는 "대칭키를 대체하려고" 나온 게 아니라 **"대칭키를 건네줄 방법을 만들려고"** 나왔다. 이 동기를 잡고 가면 나머지가 자연스럽다.

> **확인 질문**
> 1. AES-GCM 이 기밀성 외에 무엇을 더 주나?
> 2. 대칭키 암호의 유일한 구조적 약점은 무엇인가?

## 4. 공개키 암호 — 잠그는 열쇠와 여는 열쇠를 분리한다

### 4.1 두 개의 키, 서로 다른 역할

**공개키 암호(asymmetric / public-key cryptography)** 의 발상은 이거다.

> 잠그는 열쇠와 여는 열쇠를 **다른 것으로** 만들자. 그러면 잠그는 열쇠는 공개해도 된다.

- **공개키(public key)** — 전 세계에 뿌려도 된다. 인증서에 담겨 배포된다.
- **개인키(private key)** — 주인만 갖는다. 절대 밖으로 내보내지 않는다.

우편함에 비유하면 정확하다. **투입구는 길가에 공개**돼 있어서 누구나 편지를 넣을 수 있지만(공개키로 암호화), **꺼내는 열쇠는 주인만** 갖고 있다(개인키로 복호화).

### 4.2 작은 숫자로 보는 RSA

수학은 최소한만 보자. RSA 의 핵심은 **큰 수의 소인수분해가 어렵다**는 것이다.

작은 숫자로:

```
1. 소수 두 개를 고른다            p = 61,  q = 53
2. 곱한다                        n = p × q = 3233        ← 공개된다
3. 공개 지수를 고른다              e = 17                  ← 공개된다
4. 규칙에 따라 개인 지수를 계산     d = 2753                ← 비밀

   공개키 = (n=3233, e=17)
   개인키 = (n=3233, d=2753)

암호화:  암호문 = 평문^e  mod n      →  65^17   mod 3233 = 2790
복호화:  평문   = 암호문^d mod n      →  2790^2753 mod 3233 = 65
```

`n` 과 `e` 는 공개돼 있다. 그런데 `d` 를 구하려면 **`n=3233` 을 61 과 53 으로 쪼개야** 한다. 3233 은 쉽지만, 실제 RSA-2048 의 `n` 은 **617자리 십진수**다. 현재 알려진 어떤 방법으로도 현실적 시간 안에 못 쪼갠다.

> 이게 "어렵다"의 정체다. **불가능하다고 증명된 게 아니라, 아무도 빠른 방법을 못 찾았을 뿐**이다. 그래서 양자컴퓨터 이야기가 나오면 RSA 가 먼저 거론된다. 실제로 [8.3](#83-ecdh-와-ecdhe--e-하나가-바꾸는-것)에서 볼 이 장비의 TLS 협상 결과에 이미 양자내성 알고리즘이 섞여 있다.

### 4.3 공개키로 암호화하면 무엇이 보장되나

여기가 이 장의 요점이다.

> **공개키로 암호화한 것은 짝이 되는 개인키로만 열 수 있다.**
> → 따라서 **그 개인키를 가진 사람만 읽을 수 있다.** (기밀성)

공개키는 전 세계가 갖고 있으므로 **누구나 보낼 수 있다.** 대신 **받는 사람은 한 명**으로 고정된다. 방향을 정리하면:

| 연산 | 쓰는 키 | 누가 할 수 있나 | 보장하는 것 |
|---|---|---|---|
| **암호화** | 받는 사람의 **공개키** | 누구나 | 개인키 주인만 읽는다 |
| **복호화** | 자기 **개인키** | 개인키 주인만 | — |

주의할 점은 **암호화가 "보낸 사람"을 전혀 증명하지 않는다**는 것이다. 공개키는 누구나 갖고 있으니 아무나 보낼 수 있다. 보낸 사람을 증명하는 건 [6장](#6-전자서명--이-문서에서-가장-중요한-primitive)의 전자서명이고, **완전히 다른 연산**이다.

### 4.4 개인키를 지켜야 하는 이유

공개키가 유출돼도 아무 일이 없다. 원래 공개하라고 만든 것이다. 인증서에 담겨 접속하는 모든 클라이언트에게 배포된다.

개인키가 유출되면 두 가지가 동시에 무너진다.

1. **그 공개키로 암호화된 모든 것을 읽을 수 있다** — 과거에 녹음해 둔 트래픽까지 포함해서
2. **그 신원으로 서명할 수 있다** — 서버를 사칭할 수 있다

1번의 "과거 트래픽까지"가 무서운 대목이다. 공격자가 몇 년치 암호문을 모아 두었다가 나중에 개인키를 손에 넣으면 **소급해서 전부 복호화**된다. 이 위험을 없애려고 나온 개념이 [8.3](#83-ecdh-와-ecdhe--e-하나가-바꾸는-것)의 **전방 비밀성**이다.

실무에서 개인키가 어디 있는지는 항상 알고 있어야 한다. 서버 인증서의 개인키, JWT 서명 키, SSH 키 — 전부 같은 원칙이다. **평문으로 저장소에 커밋되는 사고가 가장 흔하다.**

> **확인 질문**
> 1. 공개키로 암호화했을 때 보장되는 것과 **보장되지 않는 것**은 각각 무엇인가?
> 2. 개인키가 유출되면 왜 과거 트래픽까지 위험한가?

## 5. 해시 — 되돌릴 수 없는 지문

### 5.1 복호화라는 개념 자체가 없다

**해시 함수(hash function)** 는 임의 길이 입력을 **고정 길이 요약값**으로 바꾼다. 그 결과를 **다이제스트(digest)** 라고 한다.

암호화와 결정적으로 다른 점은 **"되돌리기 어렵다"가 아니라 "되돌린다는 개념이 없다"** 는 것이다.

암호화는 원본 정보를 **전부 보존**한다. 키만 있으면 완벽히 복원된다. 해시는 **정보를 버린다.** 1MB 입력이 32바이트가 됐다면 나머지는 사라진 것이다. 복원할 정보가 애초에 남아 있지 않다.

> "해시를 복호화한다"는 말은 **"평균값에서 원래 숫자들을 복원한다"** 와 같은 종류의 말이다. 키가 없어서 못 하는 게 아니라, 그런 연산이 존재하지 않는다.

가끔 "해시가 풀렸다"는 표현을 보는데 그건 복호화가 아니라 **같은 해시가 나오는 입력을 찾아낸 것**이다(미리 계산해 둔 표를 뒤지거나, 무차별 대입으로). 원리적으로 다른 일이다.

### 5.2 입력이 아무리 커도 결과는 고정 길이

직접 확인하면 바로 납득된다.

```
$ printf 'a'     | openssl dgst -sha256
ca978112ca1bbdcafac231b39a23dc4da786eff8147c4e72b9807785afee48bb

$ printf 'hello' | openssl dgst -sha256
2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824

$ head -c 1000000 /dev/urandom | openssl dgst -sha256
8c52cd1574c95786f06c05968622354d692739f21e9d192633ee6db83ade20ff
```

1바이트든 1MB 든 **항상 64자리 16진수 = 32바이트**다. SHA-256 의 256 이 곧 출력 비트 수다.

그리고 **한 글자만 바꿔도 결과가 완전히 달라진다.** 이걸 **눈사태 효과(avalanche effect)** 라고 한다.

```
$ printf 'hello' | openssl dgst -sha256
2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824

$ printf 'hellp' | openssl dgst -sha256      ← 마지막 글자만 o → p
fdd7585e08c4e2afd71dcabdb4636c89d557a3f42db9e2040c8bbd1708aa4ce7
```

앞자리 몇 개만 비슷한 게 아니라 **전혀 무관한 값**이 된다. 이 성질 덕분에 "조금만 바꾸면 티가 안 나게 조작"하는 게 불가능하다.

### 5.3 무결성은 여기서 나온다

해시가 안전한 요약이 되려면 두 가지가 필요하다.

- **역상 저항성** — 주어진 다이제스트에서 원본을 찾아낼 수 없다
- **충돌 저항성** — 같은 다이제스트를 내는 서로 다른 입력 두 개를 찾아낼 수 없다

**충돌 저항성이 무너지면 무결성이 통째로 무너진다.** "계약서 A 와 계약서 B 의 해시가 같다"면 A 에 받은 서명을 B 에 갖다 붙일 수 있기 때문이다. 이게 MD5 와 SHA-1 이 퇴출된 이유다 — 충돌을 실제로 만들어낸 사례가 나왔다.

그래서 인증서에서 서명 알고리즘 이름에 `sha1` 이 보이면 그 자체로 문제 신호다.

**다만 해시만으로는 무결성이 완성되지 않는다.** 공격자가 데이터를 바꾸고 **해시도 같이 새로 계산해서** 붙이면 그만이기 때문이다. 해시에는 키가 없으니 누구나 계산할 수 있다. 그래서 해시는 반드시 **"공격자가 위조할 수 없는 무언가"와 결합**되어야 하고, 그게 [HMAC](#7-hmac--공유-비밀로-만드는-무결성-태그)이거나 [전자서명](#6-전자서명--이-문서에서-가장-중요한-primitive)이다.

### 5.4 비밀번호 저장은 별개의 문제다

짧게만 짚고 간다. **비밀번호 저장에 SHA-256 을 그냥 쓰면 안 된다.**

이유가 5.2 에 있다. SHA-256 은 **너무 빠르다.** 무결성 검사에는 장점이지만 비밀번호에는 치명적인 단점이다. 공격자가 유출된 다이제스트를 놓고 초당 수십억 개를 시도할 수 있다.

비밀번호에는 **일부러 느리게** 설계된 함수를 쓴다 — **bcrypt, scrypt, Argon2**. 그리고 계정마다 다른 **솔트(salt)** 를 붙여서 미리 계산해 둔 표가 통하지 않게 한다.

> 정리: **SHA-256 은 "데이터가 안 바뀌었나"에 쓰는 것이지 "비밀번호를 지키는" 용도가 아니다.** 목적이 다르면 도구도 다르다.

> **확인 질문**
> 1. "해시를 복호화한다"가 왜 말이 안 되나? 암호화와 정확히 어디서 갈리나?
> 2. 해시만 붙여서는 왜 무결성이 보장되지 않나?

## 6. 전자서명 — 이 문서에서 가장 중요한 primitive

인증서, CA, 신뢰 체인이 전부 이 하나 위에 서 있다. 여기를 정확히 잡으면 [자매 문서](tls-certificates-and-trust-stores.md)가 전부 따라온다.

### 6.1 어떤 문제를 푸나

5.3 에서 남은 문제가 있었다. 해시는 누구나 계산할 수 있어서, 데이터와 해시를 같이 바꿔치기하면 막을 수 없다.

필요한 건 **"이 요약값을 만든 게 특정한 그 사람이다"를 증명하는 방법**이다. 그러려면 그 사람만 할 수 있는 연산이어야 하고, 동시에 **누구나 검증은 할 수 있어야** 한다.

**전자서명(digital signature)** 이 정확히 그것이다.

| | 만들 수 있는 사람 | 검증할 수 있는 사람 |
|---|---|---|
| 해시 | 누구나 | 누구나 |
| **전자서명** | **개인키 주인만** | **누구나** (공개키로) |

이 비대칭이 전부다.

### 6.2 개인키로 서명하고 공개키로 검증한다

정확한 용어로 고정해 두자.

```
서명 생성 (signing)      :  데이터 + 개인키  →  서명값
서명 검증 (verification) :  데이터 + 서명값 + 공개키  →  참 / 거짓
```

**검증의 출력은 데이터가 아니라 참/거짓**이다. 이 점이 6.4 에서 중요해진다.

그림으로:

```
        [보내는 쪽]                            [받는 쪽]
                                    
   데이터 ──┐                            ┌── 데이터
            ├─ 개인키로 서명 ─> 서명값 ───┤
            ┘                            ├─ 공개키로 검증 ─> 참/거짓
                                         ┘
                                         
   개인키 있어야 가능                    공개키만 있으면 가능
```

전자서명이 보장하는 것은 **인증 + 무결성 + 부인방지**다. **기밀성은 주지 않는다** — 서명을 붙여도 데이터 자체는 평문 그대로다. 서명은 데이터를 **숨기는** 게 아니라 **보증하는** 것이다.

이게 인증서에서 그대로 보인다. 인증서는 **공개된 파일**이고 아무나 읽을 수 있다. 서명은 내용을 감추려고 붙은 게 아니라 "이 내용을 CA 가 보증한다"를 붙인 것이다.

### 6.3 왜 원본이 아니라 해시에 서명하나

실제 전자서명은 **원본 전체가 아니라 원본의 해시에** 적용된다.

```
원본 데이터 ──SHA-256──> 다이제스트(32바이트) ──서명 연산──> 서명값
```

이유는 셋이다.

1. **성능** — 공개키 연산은 3.3 에서 본 대로 매우 느리다. 1MB 파일 전체에 적용하는 건 비현실적이다.
2. **크기 제한** — 공개키 연산은 한 번에 처리 가능한 크기가 키 길이에 묶여 있다. RSA-2048 은 245바이트가 상한이다(9.1). 32바이트 다이제스트는 여유롭게 들어간다.
3. **고정 길이** — 원본 크기와 무관하게 서명 절차가 동일해진다.

여기서 5.3 의 충돌 저항성이 왜 중요한지가 다시 보인다. **서명은 사실상 다이제스트에 대한 보증**이므로, 같은 다이제스트를 갖는 다른 문서를 만들 수 있으면 서명이 그대로 옮겨 붙는다.

### 6.4 "개인키로 암호화한다"가 왜 부정확한가

전자서명을 이렇게 설명하는 자료가 아주 많다.

> ❌ "개인키로 **암호화**하고, 받는 쪽이 공개키로 **복호화**해서 원래 해시와 비교한다"

**이 표현은 RSA 에 한정된 수학적 직관이지, 전자서명 일반에 대한 설명으로는 틀렸다.** 세 가지 이유가 있다.

**첫째, 다른 서명 알고리즘에는 그런 연산이 아예 없다.**

ECDSA 와 Ed25519 는 타원곡선 기반인데, 여기에는 **"공개키로 암호화한다"는 연산 자체가 정의돼 있지 않다.** 따라서 그 역인 "개인키로 암호화"도 존재할 수 없다. 말로만 하면 안 믿기니 직접 시켜 봤다.

```
$ openssl pkeyutl -encrypt -inkey ec.pub -pubin -in msg.txt -out c.bin
pkeyutl: Error initializing context
...:digital envelope routines:evp_pkey_asym_cipher_init:
   operation not supported for this keytype:crypto/evp/asymcipher.c:202:

$ openssl pkeyutl -encrypt -inkey ed.pub -pubin -in msg.txt -out c.bin
pkeyutl: Error initializing context
...:operation not supported for this keytype:crypto/evp/asymcipher.c:202:
```

**`operation not supported for this keytype`** — 키가 약해서도, 옵션이 틀려서도 아니다. **그 키 종류에는 암호화라는 연산이 없다.**

그런데 같은 키로 **서명은 아무 문제 없이 된다.**

```
$ openssl pkeyutl -sign -inkey ed.key -rawin -in msg.txt -out ed.sig
  Ed25519 서명 생성: OK (64바이트)
$ openssl pkeyutl -verify -inkey ed.pub -pubin -rawin -in msg.txt -sigfile ed.sig
  Signature Verified Successfully

$ openssl dgst -sha256 -sign ec.key -out ec.sig msg.txt
  ECDSA 서명 생성: OK (71바이트)
$ openssl dgst -sha256 -verify ec.pub -signature ec.sig msg.txt
  Verified OK
```

**암호화는 불가능하고 서명은 가능하다.** 이 한 쌍의 결과가 "서명 = 개인키 암호화"라는 설명을 결정적으로 반박한다. 서명은 암호화의 변형이 아니라 **독립된 primitive** 다.

**둘째, 검증의 출력이 데이터가 아니다.**

"복호화해서 원래 해시와 비교한다"는 서술은 **검증 과정에서 해시가 튀어나온다**고 말한다. RSA PKCS#1 v1.5 에서는 실제로 그 비슷한 일이 일어난다. 하지만 ECDSA 검증은 **서명값과 공개키로 수식을 계산해 조건이 성립하는지 보는 것**이고, 중간에 "복원된 해시" 같은 건 나오지 않는다. 출력은 **참/거짓뿐**이다.

**셋째, 목적이 반대다.**

암호화는 **읽지 못하게** 하는 것이고 서명은 **누가 만들었는지 밝히는** 것이다. 서명된 데이터는 여전히 평문이다. "암호화"라는 단어를 쓰면 기밀성을 준다는 오해가 생긴다.

**그래서 이렇게 쓴다.**

| ❌ 부정확 | ✅ 정확 |
|---|---|
| 개인키로 암호화 → 공개키로 복호화 | **개인키로 서명 → 공개키로 검증** |
| 서명을 공개키로 되돌려 해시를 얻는다 | 서명·데이터·공개키로 **검증에 성공/실패한다** |
| 한쪽 키로 처리한 건 다른 쪽 키로만 되돌린다 | **공개키 암호화 → 개인키 복호화** (암호화의 방향) <br> **개인키 서명 → 공개키 검증** (서명의 방향) |

두 문장을 나란히 외워 두면 헷갈릴 일이 없다.

```
Public key encryption  →  Private key decryption      (기밀성)
Private key signing    →  Public key verification     (인증·무결성)
```

RSA 안에서는 왜 그 잘못된 비유가 그럴듯하게 들리는지는 [부록 A.1](#a1-rsa-에서-서명과-암호화가-왜-닮아-보이나)에 정리했다.

### 6.5 RSA · ECDSA · Ed25519 는 어떻게 다른가

| | **RSA** | **ECDSA** | **Ed25519** |
|---|---|---|---|
| 기반 문제 | 소인수분해 | 타원곡선 이산로그 | 타원곡선 (Edwards) |
| **암호화 가능** | **가능** | **불가능** | **불가능** |
| 서명 | 가능 | 가능 | 가능 |
| 2048비트급 키 크기 | 256바이트 | 32바이트 (P-256) | 32바이트 |
| 서명값 크기 (실측) | 256바이트 | 71바이트 | **64바이트** |
| 같은 입력 재서명 | **매번 같음** (PKCS#1 v1.5) | **매번 다름** | 매번 같음 (결정적) |
| 구현 사고 위험 | 패딩 선택 | **난수 재사용 시 개인키 노출** | 설계상 방지됨 |

**RSA 만 암호화가 가능하다**는 첫 행이 이 표의 핵심이다. RSA 는 **암호화도 되고 서명도 되는** 알고리즘이고, 나머지 둘은 **서명 전용**이다. "공개키 암호"라는 말을 들으면 RSA 를 떠올리게 되는데, 그 RSA 가 오히려 예외적인 만능형이다.

재서명 결과가 다른 것도 직접 확인된다.

```
# RSA (PKCS#1 v1.5) — 두 번 서명해도 같다
Krz8bhuKEpDjZrqAQmdSHouw4Bl1Hc/t1PrD5+92
Krz8bhuKEpDjZrqAQmdSHouw4Bl1Hc/t1PrD5+92

# ECDSA — 두 번 서명하면 다르다
MEUCIQD3dIXH6i/sVz3fz9ZbDQVLbpDWWiLtuUVB
MEQCH0qDINSuLHbnfQTz0X2akJLLeCJ7A54ZcPL/
```

ECDSA 가 매번 다른 이유는 서명할 때마다 **일회용 난수**를 쓰기 때문이다. 이 난수가 재사용되면 **개인키가 수학적으로 계산되어 버린다.** 실제로 이 실수로 게임 콘솔 서명 키가 통째로 털린 유명한 사건이 있다. Ed25519 는 이 난수를 개인키와 메시지로부터 결정적으로 유도해서 **구현자가 실수할 여지 자체를 없앴다.**

실무 선택 기준은 단순하다. **새로 만들면 Ed25519 또는 ECDSA**(키·서명이 작고 빠르다). **RSA 는 호환성 때문에 남아 있다** — 오래된 클라이언트, 특정 하드웨어 모듈 등.

### 6.6 암호화와 서명은 다른 primitive 다

2.2 에서 예고한 구분을 여기서 닫는다.

| | **공개키 암호화** | **전자서명** |
|---|---|---|
| 목적 | 기밀성 | 인증 · 무결성 · 부인방지 |
| 보내는 쪽이 쓰는 키 | 받는 사람의 **공개키** | 자기 **개인키** |
| 받는 쪽이 쓰는 키 | 자기 **개인키** | 보낸 사람의 **공개키** |
| 누가 연산할 수 있나 | 암호화는 **누구나** | 서명은 **개인키 주인만** |
| 출력 | 암호문 (원본 복원 가능) | 서명값 (원본은 평문 그대로) |
| 검증 결과 | 복원된 원본 | **참/거짓** |
| 지원 알고리즘 | RSA | RSA, ECDSA, Ed25519 |

같은 RSA 키 쌍으로 둘 다 할 수는 있지만, **실무에서는 용도를 분리하는 게 원칙**이다. 인증서의 `Key Usage` 확장이 바로 그 용도를 못 박는 필드다 — `keyEncipherment`(암호화용)와 `digitalSignature`(서명용)가 따로 있는 이유다. ([자매 문서 3.2](tls-certificates-and-trust-stores.md#32-인증서-안에-실제로-들어-있는-것) 참고)

> **확인 질문**
> 1. "개인키로 암호화한다"가 Ed25519 에서 왜 불가능한가? 어떻게 확인할 수 있나?
> 2. 전자서명이 보장하는 세 가지와, 보장하지 **않는** 하나는 무엇인가?
> 3. ECDSA 에서 같은 메시지를 두 번 서명하면 왜 결과가 다른가?

## 7. HMAC — 공유 비밀로 만드는 무결성 태그

### 7.1 해시만으로는 왜 부족한가

5.3 의 문제를 다시 보자.

```
보내는 쪽:  데이터 + SHA256(데이터)  ──전송──>  받는 쪽: 해시 다시 계산해 비교

공격자:  데이터를 바꾸고 SHA256(바뀐 데이터) 도 같이 바꿔 붙인다  → 검사 통과
```

해시에는 **키가 없어서 누구나 계산할 수 있다**는 게 문제다. 그러면 **공격자는 모르는 비밀을 해시 계산에 섞으면** 된다.

### 7.2 HMAC 이 하는 일

**HMAC(Hash-based Message Authentication Code)** 은 **해시 + 공유 비밀키**다. 결과값을 **MAC** 또는 **태그(tag)** 라고 부른다.

```
              [공유 비밀키 K]
                    |
   데이터 ──────────┴──> HMAC-SHA256 ──> 태그(32바이트)
```

키를 모르면 올바른 태그를 만들 수 없다. 같은 데이터라도 키가 다르면 태그가 전혀 다르다.

```
$ printf 'hello' | openssl dgst -sha256 -hmac "key-A"
b7a5e344c726cdc2d92ee8724bfff6b8a600d50329cf00f1d00bdfd71296ba8d

$ printf 'hello' | openssl dgst -sha256 -hmac "key-B"
e8ffb68ee47d3b66e452555e7ea699182ef62be375597faf80f7844c2b7b73c0
```

키를 모르는 공격자는 데이터를 바꾼 뒤 맞는 태그를 만들 수 없다. **무결성 + 인증**이 확보된다.

> 참고로 `SHA256(키 + 데이터)` 처럼 단순히 이어붙이는 방식은 **길이 확장 공격(length extension attack)** 에 취약하다. HMAC 은 키를 두 번, 안팎으로 섞는 구조로 이를 막는다. **직접 만들지 말고 HMAC 을 쓰라**는 것이 실무 원칙이다.

### 7.3 전자서명과 무엇이 다른가

둘 다 무결성과 인증을 주지만 **키 구조가 근본적으로 다르다.**

| | **해시** | **HMAC** | **전자서명** |
|---|---|---|---|
| 키 | 없음 | **공유 비밀 1개** | **키 쌍** (개인·공개) |
| 만들 수 있는 사람 | 누구나 | 키를 아는 **양쪽 모두** | **개인키 주인만** |
| 검증할 수 있는 사람 | 누구나 | 키를 아는 **양쪽 모두** | **누구나** (공개키로) |
| 기밀성 | 없음 | 없음 | 없음 |
| 무결성 | 조건부 | 있음 | 있음 |
| 인증 | 없음 | 있음 | 있음 |
| **부인방지** | 없음 | **없음** | **있음** |
| 속도 | 매우 빠름 | 매우 빠름 | 느림 |
| 키 배포 | — | 사전에 안전하게 공유 필요 | 공개키만 뿌리면 됨 |

**부인방지 칸이 결정적인 차이**다. HMAC 은 양쪽이 같은 키를 갖고 있으니, A 가 만든 태그를 B 도 똑같이 만들 수 있다. 그래서 분쟁이 생겼을 때 **제3자에게 "이건 A 가 만든 것"이라고 증명할 수 없다.** 전자서명은 개인키 주인만 만들 수 있으므로 증명이 된다.

**키 배포 칸도 실무적으로 크다.** HMAC 은 통신 상대마다 비밀키를 미리 안전하게 나눠 가져야 하는데, 상대가 100개면 100개를 관리해야 한다. 전자서명은 **공개키를 공개해 두면** 누구나 검증한다. 인증서 체계가 전자서명 위에 서 있는 이유다.

### 7.4 JWT 의 HS256 과 RS256

백엔드 개발자가 이 차이를 매일 마주치는 곳이 **JWT(JSON Web Token)** 다.

JWT 는 `헤더.페이로드.서명` 세 부분이 점으로 이어진 문자열이다. 앞 두 부분은 **Base64 로 인코딩됐을 뿐 암호화가 아니다** — 누구나 디코딩해서 읽을 수 있다. 세 번째가 위조를 막는다.

| | **HS256** | **RS256** (또는 ES256) |
|---|---|---|
| 방식 | **HMAC**-SHA256 | **전자서명** (RSA / ECDSA) |
| 키 | 공유 비밀 1개 | 키 쌍 |
| 발급자 | 비밀키 보유 | **개인키** 보유 |
| 검증자 | **같은 비밀키** 필요 | **공개키만** 있으면 됨 |
| 검증자가 토큰을 위조할 수 있나 | **할 수 있다** | **할 수 없다** |

여기서 실무 판단이 갈린다.

- **단일 서비스가 발급도 하고 검증도 한다** → HS256 으로 충분하다. 빠르고 단순하다.
- **발급자와 검증자가 다르다** (인증 서버 하나 + 마이크로서비스 여럿, 또는 외부 파트너가 검증) → **반드시 RS256/ES256.** HS256 을 쓰면 **검증만 하면 되는 모든 서비스에 위조 능력까지 같이 나눠주는 꼴**이다. 서비스 하나가 털리면 전체 인증이 무너진다.

> **JWT 의 가장 유명한 취약점**도 이 구분에서 나온다. 토큰 헤더의 `alg` 필드를 서버가 그대로 믿으면, 공격자가 `alg` 를 `none` 으로 바꾸거나 RS256 을 HS256 으로 바꿔치기해서 **공개키를 HMAC 키로 사용**해 토큰을 위조할 수 있다. 공개키는 누구나 갖고 있으니까. **검증 시 알고리즘은 서버가 고정해야 하고, 토큰이 말하는 대로 따라가면 안 된다.**

> **확인 질문**
> 1. HMAC 과 전자서명 중 부인방지를 주는 쪽은 어디이고 왜인가?
> 2. 마이크로서비스 여럿이 같은 JWT 를 검증한다면 HS256 과 RS256 중 무엇을 골라야 하고 왜인가?

## 8. 키 교환 — 비밀을 보내지 않고 비밀을 공유하는 법

### 8.1 암호화 알고리즘과 키 교환 알고리즘은 다르다

3.4 의 순환 문제로 돌아간다. 대칭키가 필요한데 그 키를 건네줄 안전한 통로가 없었다.

떠오르는 해법은 **"공개키로 대칭키를 암호화해서 보낸다"** 이다. 실제로 옛날 TLS 가 그렇게 했다. 그런데 이건 **키 전송(key transport)** 이고, 4.4 에서 본 문제가 있다 — 서버 개인키가 나중에 유출되면 **과거에 녹음해 둔 트래픽의 대칭키가 전부 복호화**된다.

그래서 다른 접근이 주류가 됐다.

> **아무도 키를 보내지 않는다. 양쪽이 각자 계산해서 같은 값에 도달한다.**

이게 **키 교환(key exchange / key agreement)** 이다. 이름이 헷갈리는데 **"키를 주고받는 것"이 아니라 "키를 합의해서 만들어 내는 것"** 이다.

| | **암호화 알고리즘** | **키 교환 알고리즘** |
|---|---|---|
| 하는 일 | 데이터를 감춘다 | **공유 비밀을 합의한다** |
| 입력 | 평문 + 키 | 각자의 임시 키 쌍 |
| 출력 | 암호문 | **양쪽이 같은 값** (공유 비밀) |
| 예 | AES, ChaCha20, RSA | **DH, ECDH, ECDHE** |
| 데이터를 암호화하나 | 한다 | **하지 않는다** |

**ECDHE 는 데이터를 하나도 암호화하지 않는다.** 대칭키를 만들어 낼 뿐이고, 실제 암호화는 그 키로 AES 가 한다. 이 역할 분담이 10장 그림의 뼈대다.

### 8.2 색을 섞는 비유

**디피–헬먼(Diffie–Hellman, DH)** 의 아이디어는 색깔로 설명하는 게 가장 직관적이다.

```
         앨리스                  공개 채널                  밥
                          
   공통 색(노랑) ─────────── 공개돼 있음 ───────────── 공통 색(노랑)
                          
   비밀 색(빨강)                                        비밀 색(파랑)
        |                                                   |
   노랑+빨강=주황  ───────> 주황, 초록 <───────  노랑+파랑=초록
        |            (도청자는 이 둘을 본다)                 |
        v                                                   v
   초록+빨강 = 갈색                                   주황+파랑 = 갈색
                          
              양쪽 모두 같은 갈색에 도달  ← 이게 공유 비밀
```

핵심은 **섞기는 쉽지만 분리하기는 어렵다**는 성질이다. 도청자는 노랑·주황·초록을 다 보지만, 주황에서 빨강을 **분리해낼 수 없어서** 갈색을 만들지 못한다.

실제로는 색 대신 수학 연산(거듭제곱 나머지, 또는 타원곡선 위의 점 곱셈)을 쓰고, "분리하기 어렵다"가 **이산로그 문제**다.

여기서 중요한 것은 **갈색(공유 비밀)이 네트워크에 단 한 번도 흐르지 않는다**는 점이다. 양쪽이 각자 계산해서 도달했을 뿐이다. 그래서 나중에 서버 개인키가 유출돼도 이미 지난 세션의 키는 복원되지 않는다.

### 8.3 ECDH 와 ECDHE — E 하나가 바꾸는 것

이름이 비슷해서 헷갈린다.

| 약자 | 풀이 | 키 쌍 |
|---|---|---|
| **DH** | Diffie–Hellman | 큰 수 기반, 느리고 키가 큼 |
| **ECDH** | Elliptic Curve DH | 타원곡선 기반. 빠르고 키가 작음. **고정 키** |
| **ECDHE** | ECDH **Ephemeral** | 타원곡선 기반. **접속마다 새 임시 키** |

마지막 **E = Ephemeral(일시적)** 하나가 결정적이다.

- **ECDH** — 양쪽이 고정된 키 쌍을 계속 쓴다. 그 키가 유출되면 **그 키로 맺은 모든 과거 세션**이 복원된다.
- **ECDHE** — **접속할 때마다 키 쌍을 새로 만들고 끝나면 버린다.** 세션이 끝나면 그 세션의 비밀을 복원할 재료가 세상에서 사라진다.

이 성질이 **전방 비밀성(Forward Secrecy)** 이다. 4.4 에서 예고한 "과거 트래픽 소급 복호화" 위험을 없애는 장치다.

> **오늘 서버의 개인키가 통째로 유출돼도, 어제 오간 트래픽은 복호화되지 않는다.**

서버 개인키는 이제 **신원을 증명하는 데만**(서명) 쓰이고, **대칭키를 만드는 데는 쓰이지 않는다.** 역할이 분리된 것이다.

TLS 1.3 은 아예 **키 전송 방식(정적 RSA)을 제거하고 (EC)DHE 만 남겼다.** 그 흔적이 3.2 에서 본 암호 스위트 목록에 그대로 드러난다.

```
TLS 1.2 :  AES256-GCM-SHA384   Kx=RSA  Au=RSA   ← 키 교환이 스위트 이름에 박혀 있다
TLS 1.3 :  TLS_AES_256_GCM_SHA384  Kx=any  Au=any  ← 스위트에서 아예 빠졌다
```

TLS 1.3 의 암호 스위트는 **대칭 암호와 해시만 지정**한다. 키 교환과 인증은 별도로 협상된다. 그래서 `Kx=any Au=any` 다.

실제 협상 결과를 보면 이렇다.

```
$ openssl s_client -connect localhost:8443 ... | grep -iE "group|protocol|cipher is"
Negotiated TLS1.3 group: X25519MLKEM768
New, TLSv1.3, Cipher is TLS_AES_256_GCM_SHA384
Protocol: TLSv1.3
```

**키 교환 그룹이 암호 스위트와 따로 표시된다.** 그리고 여기 잡힌 `X25519MLKEM768` 은 기존 타원곡선(X25519)과 **양자내성 알고리즘(ML-KEM-768)을 결합한 하이브리드**다. 4.2 에서 말한 "언젠가 소인수분해가 깨지면"에 대한 대비가 이미 기본값으로 들어와 있다는 뜻이다. 키 교환이 스위트에서 분리된 덕분에 **암호 스위트를 건드리지 않고 이 축만 갈아끼울 수 있었다.**

> **확인 질문**
> 1. ECDHE 는 데이터를 암호화하나? 그럼 무엇을 하나?
> 2. 전방 비밀성이란 무엇이고, ECDH 와 ECDHE 중 어느 쪽이 제공하나?

## 9. 하이브리드 암호 — 왜 RSA 로 전부 암호화하지 않나

### 9.1 세 가지 이유

"공개키 암호가 키 전달 문제를 풀었다면, 그냥 전부 공개키로 암호화하면 되지 않나?" 안 되는 이유가 셋이다.

**1. 너무 느리다.** 3.3 의 실측 — AES 초당 2.3 GB vs RSA 초당 약 0.76 MB. **약 3,000배 차이**다. 이미지 하나 받는 데 몇 분이 걸린다.

**2. 크기 제한이 있다.** 공개키 암호는 **키 길이보다 큰 데이터를 한 번에 처리할 수 없다.** RSA-2048(256바이트 키)의 실제 경계를 재 봤다.

```
padding=pkcs1              padding=oaep
  245바이트 → 성공           214바이트 → 성공
  246바이트 → 실패           245바이트 → 실패
```

**PKCS#1 v1.5 는 245바이트, OAEP 는 214바이트가 상한**이다(패딩이 자리를 차지한다). 웹페이지 하나도 못 담는다. 나눠서 처리하면 되지 않냐 싶지만, 그러면 1번 문제가 수천 배로 커진다.

**3. 전방 비밀성이 없다.** 8.3 에서 본 대로다. 서버 개인키로 모든 걸 암호화하면 그 키 하나가 유출됐을 때 **과거 전부**가 열린다.

### 9.2 그래서 실제 조합은 이렇게 된다

각자 잘하는 일만 시킨다. 이걸 **하이브리드 암호(hybrid encryption)** 라고 한다.

```
   ┌──────────────────────────────────────────────────────────┐
   │ 1. 공개키 암호 / 키 교환                                  │
   │    ECDHE 로 양쪽이 같은 값에 도달 (또는 RSA 로 비밀 전달) │
   │    → 느리지만 작은 데이터 한 번만                         │
   └──────────────────────┬───────────────────────────────────┘
                          ↓
   ┌──────────────────────────────────────────────────────────┐
   │ 2. 공유 비밀(shared secret) 확보                          │
   │    네트워크에 흐른 적 없는 값                             │
   └──────────────────────┬───────────────────────────────────┘
                          ↓
   ┌──────────────────────────────────────────────────────────┐
   │ 3. 세션 키 유도                                           │
   │    공유 비밀 → 키 유도 함수(KDF) → 방향별 대칭키          │
   └──────────────────────┬───────────────────────────────────┘
                          ↓
   ┌──────────────────────────────────────────────────────────┐
   │ 4. 실제 데이터 암호화                                     │
   │    AES-GCM / ChaCha20-Poly1305 로 전부 처리               │
   │    → 빠르고 크기 제한 없고 무결성까지                     │
   └──────────────────────────────────────────────────────────┘
```

> **"비싼 연산은 딱 한 번, 작은 데이터에만. 나머지는 전부 싼 연산으로."**

이 패턴은 TLS 밖에서도 똑같이 나온다. 파일 암호화 도구도, 클라우드의 봉투 암호화(envelope encryption)도 구조가 같다 — 데이터는 대칭키로 암호화하고, 그 대칭키만 공개키로 감싼다.

> **확인 질문**
> 1. RSA 로 HTTP 응답 전체를 암호화하지 않는 이유 세 가지를 말해 보라.
> 2. 이 흐름에서 "비싼 연산"은 어디에 딱 한 번 쓰이나?

## 10. 여기까지를 TLS 한 장으로

### 10.1 큰 그림

배운 primitive 가 TLS 안에서 어떻게 맞물리는지 한 장으로 본다.

```
        Browser                                             Server
           |                                                   |
           |  ① ClientHello                                    |
           | ────────────────────────────────────────────────> |
           |     지원하는 암호 스위트 · 키 교환 그룹 목록       |
           |                                                   |
           |  ② Certificate                                    |
           | <──────────────────────────────────────────────── |
           |     서버 공개키 + 신원(도메인) + CA 의 서명        |
           |                                                   |
    ┌──────┴──────────────────────────────────┐                |
    │ ③ 인증서 검증                            │                |
    │   · CA 서명 검증        ← 전자서명 (6장) │                |
    │   · 체인을 신뢰 앵커까지                 │                |
    │   · 도메인 · 유효기간                    │  ← 자매 문서    |
    └──────┬──────────────────────────────────┘                |
           |                                                   |
           |  ④ CertificateVerify                              |
           | <──────────────────────────────────────────────── |
           |     핸드셰이크 기록에 대한 서버의 서명             |
           |     → 인증서 개인키를 "실제로 갖고 있음" 증명      |
           |                                                   |
           |  ⑤ Key Exchange (ECDHE)                           |
           | <────────────────────────────────────────────────>|
           |     각자 임시 키 쌍. 공유 비밀은 전송되지 않음     |
           |                                                   |
           |  ===== 양쪽이 동일한 session key 유도 (KDF) ===== |
           |                                                   |
           |  ⑥ Application Data                               |
           | <═══════════════════════════════════════════════> |
           |     AES-GCM / ChaCha20-Poly1305 (암호화 + 무결성) |
           |                                                   |
```

**④번이 자주 빠뜨리는 단계**다. 인증서는 **공개된 파일**이라 누구나 복사할 수 있다. 그래서 인증서를 내미는 것만으로는 부족하고, **그 인증서의 개인키를 실제로 갖고 있다**는 걸 별도로 증명해야 한다. 그 증명이 핸드셰이크 기록에 대한 서명이고, 이게 없으면 **인증서를 훔쳐다 붙이기만 하면 사칭이 된다.**

실제 이 장비의 핸드셰이크에서 그 서명 방식이 그대로 잡힌다.

```
Peer signature type: rsa_pss_rsae_sha256
```

### 10.2 각 primitive 가 맡은 자리

| primitive | TLS 안에서의 역할 | 없으면 |
|---|---|---|
| **해시** (5장) | 서명 대상 요약, KDF, 핸드셰이크 기록 | 서명이 불가능하게 느려짐 |
| **전자서명** (6장) | ② CA 가 인증서를 보증 · ④ 서버가 개인키 소유 증명 | **사칭을 막을 수 없음** |
| **키 교환** (8장) | ⑤ 공유 비밀 합의 | 대칭키를 안전히 못 만듦 |
| **대칭키 암호** (3장) | ⑥ 실제 데이터 암·복호화 | 느려서 실용 불가 |
| **AEAD** (3.2) | ⑥ 암호화와 동시에 무결성 | 변조를 못 잡음 |
| **인증서 + CA** | ② 그 공개키가 그 도메인 것임을 보증 | **다음 절의 문제** |

### 10.3 남은 질문 하나 — 그 공개키가 진짜 그 서버 것인가

위 흐름을 다시 보면 구멍이 하나 있다.

②에서 서버가 공개키를 보내왔다. ④에서 그 개인키를 갖고 있다는 것도 증명했다. ⑤에서 키 교환도 했다. **전부 수학적으로 정상이다.**

그런데 **중간자가 자기 키 쌍으로 똑같이 하면 어떻게 되나?** 중간자도 자기 공개키를 보내고, 자기 개인키로 서명하고, 키 교환을 한다. 모든 검증이 통과한다. 암호화도 완벽하다.

> **기밀성은 완벽한데 상대가 누구인지는 여전히 모른다.** 1.1 에서 말한 "네 가지는 서로를 포함하지 않는다"가 바로 이것이다.

그래서 마지막 질문이 남는다.

> **"이 공개키가 진짜 `api.example.com` 의 것"임을 어떻게 아는가?**

이 질문에 답하는 장치가 **인증서와 CA** 이고, 그게 자매 문서의 주제다.

> → **[TLS 인증서와 신뢰 저장소](tls-certificates-and-trust-stores.md)** 로 이어진다.

## 11. 스스로 답해 보기

| # | 질문 | 해당 섹션 |
|---|---|---|
| 1 | AES 와 RSA 는 무엇이 다른가? | [2.2](#22-세-가지는-서로-다른-축이다) · [3장](#3-대칭키-암호--빠르지만-키를-건네줄-방법이-없다) · [4장](#4-공개키-암호--잠그는-열쇠와-여는-열쇠를-분리한다) |
| 2 | RSA 와 SHA-256 은 무엇이 다른가? | [2.2](#22-세-가지는-서로-다른-축이다) · [5장](#5-해시--되돌릴-수-없는-지문) |
| 3 | 해시는 왜 복호화하지 않는가? | [5.1](#51-복호화라는-개념-자체가-없다) |
| 4 | 공개키로 암호화하면 왜 개인키 소유자만 읽을 수 있는가? | [4.3](#43-공개키로-암호화하면-무엇이-보장되나) |
| 5 | 개인키로 "암호화"한다는 표현이 왜 부정확한가? | [6.4](#64-개인키로-암호화한다가-왜-부정확한가) |
| 6 | 전자서명은 무엇을 보장하는가? 보장하지 않는 것은? | [6.2](#62-개인키로-서명하고-공개키로-검증한다) · [6.6](#66-암호화와-서명은-다른-primitive-다) |
| 7 | 해시 · HMAC · 전자서명의 차이는? | [7.3](#73-전자서명과-무엇이-다른가) |
| 8 | HTTPS 는 왜 RSA 하나만 사용하지 않는가? | [9.1](#91-세-가지-이유) |
| 9 | ECDHE 와 AES 는 각각 어떤 역할을 하는가? | [8.1](#81-암호화-알고리즘과-키-교환-알고리즘은-다르다) · [9.2](#92-그래서-실제-조합은-이렇게-된다) |
| 10 | 인증서는 이 구조에서 무엇을 해결하는가? | [10.3](#103-남은-질문-하나--그-공개키가-진짜-그-서버-것인가) · [자매 문서](tls-certificates-and-trust-stores.md) |

## 12. 기억할 문장

- 기밀성·무결성·인증·부인방지는 **서로를 포함하지 않는다.** 완벽히 암호화돼도 상대가 누군지는 모를 수 있다.
- "단방향 키"라는 건 없다 — **해시에는 키가 없다.** 키 쓰는 해시는 HMAC 이다.
- 공개키 암호는 대칭키를 **대체하려고** 나온 게 아니라 **건네주려고** 나왔다.
- **해시는 정보를 버린다.** 복호화가 어려운 게 아니라 복원할 정보가 없다.
- 전자서명의 방향은 **개인키로 서명 → 공개키로 검증**. "개인키로 암호화"는 RSA 에만 통하는 비유이고 ECDSA·Ed25519 에는 **그 연산이 존재하지 않는다.**
- 전자서명 검증의 출력은 데이터가 아니라 **참/거짓**이다.
- **HMAC 은 부인방지를 못 준다** — 양쪽이 같은 키를 갖고 있으니 제3자에게 증명할 수 없다.
- JWT 는 **발급자와 검증자가 다르면 RS256**. HS256 은 검증자에게 위조 능력까지 준다.
- **키 교환은 데이터를 암호화하지 않는다.** 대칭키를 만들어 낼 뿐이다.
- **비싼 연산은 한 번, 작은 데이터에만.** 나머지는 전부 대칭키로.

---

## 부록 A

### A.1 RSA 에서 서명과 암호화가 왜 닮아 보이나

[6.4](#64-개인키로-암호화한다가-왜-부정확한가)에서 "개인키로 암호화"라는 표현이 부정확하다고 했다. 그런데 **왜 그 표현이 그토록 널리 퍼졌는지**를 알아 두면 오히려 덜 헷갈린다. RSA 안에서는 실제로 비슷해 보이기 때문이다.

4.2 의 RSA 연산을 다시 보자.

```
암호화:  c = m^e mod n      (e = 공개 지수)
복호화:  m = c^d mod n      (d = 개인 지수)
```

두 연산이 **형태가 같다.** 지수만 바뀐다. 그래서 순서를 뒤집어도 수식이 성립한다.

```
m^d mod n  을 계산한 뒤,  그 결과를 ^e mod n 하면  다시 m 이 나온다
```

**RSA PKCS#1 v1.5 서명**이 대략 이 구조다. 그래서 "개인키로 암호화하고 공개키로 복호화한다"는 서술이 RSA 안에서는 **수학적 직관으로 통한다.** 검증 과정에서 실제로 다이제스트 비슷한 게 복원되기도 한다.

문제는 이게 **RSA 의 특수한 성질**이라는 점이다. 일반화하면 세 군데서 깨진다.

| | RSA 에서는 | 일반적으로는 |
|---|---|---|
| 공개키 암호화 연산 | 있다 | **ECDSA·Ed25519 에는 없다** |
| 검증 시 무언가 복원됨 | PKCS#1 v1.5 는 그렇게 보인다 | **ECDSA 는 참/거짓만 나온다** |
| 서명 = 암호화의 역연산 | 형태가 닮았다 | **별개의 primitive 다** |

게다가 **현대 RSA 서명 표준인 RSA-PSS 는 이 직관조차 통하지 않는다.** PSS 는 난수를 섞는 구조라 같은 메시지도 매번 다른 서명이 나오고, "복호화하면 해시가 나온다"는 그림이 성립하지 않는다. 10.1 의 실측에서 잡힌 `rsa_pss_rsae_sha256` 이 바로 그것이다.

> **정리**: "개인키로 암호화"는 **RSA PKCS#1 v1.5 의 내부 수식을 설명할 때만** 쓸 수 있는 비유다. 전자서명 일반을 말할 때는 **서명(sign) / 검증(verify)** 이라고 해야 한다.

**서명 생성·검증을 정확히 서술하면 이렇다.**

```
서명 생성:
  1. 원본을 해시한다               SHA-256 → 32바이트 다이제스트
  2. 다이제스트와 개인키를 서명 알고리즘에 넣는다
  3. 서명값이 나온다

서명 검증:
  1. 받은 원본을 같은 방식으로 해시한다
  2. 다이제스트 + 서명값 + 공개키를 검증 알고리즘에 넣는다
  3. 참 / 거짓이 나온다
```

**왜 위조가 불가능한가**: 2번의 서명 알고리즘은 **개인키 없이는 올바른 값을 만들어낼 수 없도록** 설계돼 있다. 4.2 의 소인수분해, 또는 타원곡선 이산로그 문제가 그 근거다.

**왜 해시를 먼저 쓰는가**: [6.3](#63-왜-원본이-아니라-해시에-서명하나) 참고 — 성능, 크기 제한, 고정 길이.

### A.2 실습 명령 모음

이 문서의 주장을 손으로 확인하는 명령들이다. 전부 로컬에서 끝나고 외부 네트워크가 필요 없다.

```bash
OS=openssl   # 가능하면 LibreSSL 이 아닌 OpenSSL (A.3 참고)
```

**해시 (5장)**

```bash
# 입력 크기와 무관하게 고정 길이
printf 'a'     | $OS dgst -sha256
printf 'hello' | $OS dgst -sha256
head -c 1000000 /dev/urandom | $OS dgst -sha256

# 눈사태 효과 — 한 글자만 바꿔 보기
printf 'hello' | $OS dgst -sha256
printf 'hellp' | $OS dgst -sha256
```

**HMAC (7장)**

```bash
# 같은 데이터, 다른 키 → 전혀 다른 태그
printf 'hello' | $OS dgst -sha256 -hmac "key-A"
printf 'hello' | $OS dgst -sha256 -hmac "key-B"
```

**RSA 의 크기 한계 (9.1)**

```bash
$OS genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -out rsa.key
$OS pkey -in rsa.key -pubout -out rsa.pub

for n in 214 245 246; do
  head -c $n /dev/zero > m.bin
  if $OS pkeyutl -encrypt -inkey rsa.pub -pubin -pkeyopt rsa_padding_mode:pkcs1 \
       -in m.bin -out c.bin 2>/dev/null; then echo "$n bytes: OK"; else echo "$n bytes: FAIL"; fi
done
# pkcs1 → 245 까지, oaep → 214 까지
```

**EC·Ed25519 에는 암호화 연산이 없다 (6.4 — 이 문서에서 가장 중요한 실습)**

```bash
$OS genpkey -algorithm EC -pkeyopt ec_paramgen_curve:P-256 -out ec.key
$OS pkey -in ec.key -pubout -out ec.pub
$OS genpkey -algorithm Ed25519 -out ed.key
$OS pkey -in ed.key -pubout -out ed.pub
printf 'secret message' > msg.txt

# 암호화 시도 → operation not supported for this keytype
$OS pkeyutl -encrypt -inkey ec.pub -pubin -in msg.txt -out c.bin
$OS pkeyutl -encrypt -inkey ed.pub -pubin -in msg.txt -out c.bin

# 그런데 서명은 문제없이 된다
$OS pkeyutl -sign   -inkey ed.key -rawin -in msg.txt -out ed.sig
$OS pkeyutl -verify -inkey ed.pub -pubin -rawin -in msg.txt -sigfile ed.sig
$OS dgst -sha256 -sign   ec.key -out ec.sig msg.txt
$OS dgst -sha256 -verify ec.pub -signature ec.sig msg.txt
```

**서명의 결정성 차이 (6.5)**

```bash
# RSA PKCS#1 v1.5 — 두 번 서명해도 같다
$OS dgst -sha256 -sign rsa.key msg.txt | $OS base64 -A | cut -c1-40
$OS dgst -sha256 -sign rsa.key msg.txt | $OS base64 -A | cut -c1-40

# ECDSA — 두 번 서명하면 다르다 (일회용 난수)
$OS dgst -sha256 -sign ec.key msg.txt | $OS base64 -A | cut -c1-40
$OS dgst -sha256 -sign ec.key msg.txt | $OS base64 -A | cut -c1-40
```

**대칭키 vs 공개키 속도 (3.3, 9.1)**

```bash
$OS speed -seconds 1 -elapsed aes-256-cbc
$OS speed -seconds 1 -elapsed rsa2048
```

**TLS 1.3 의 암호 스위트 (3.2, 8.3)**

```bash
$OS ciphers -s -tls1_3 -v      # 셋뿐. 전부 Kx=any Au=any Mac=AEAD
$OS ciphers -s -tls1_2 -v 'kRSA' | head -5   # TLS 1.2 에는 Kx=RSA 가 있다
```

실제 핸드셰이크에서 협상 결과를 보려면 자매 문서의 [미니 PKI 실습](tls-certificates-and-trust-stores.md#13-직접-해보기--미니-pki-실습)으로 로컬 서버를 띄운 뒤 접속해 보면 된다.

### A.3 이 실습에서 걸린 함정들

**macOS 기본 `openssl` 은 LibreSSL 이다.** `/usr/bin/openssl` 은 OpenSSL 이 아니라 Apple LibreSSL 이라 옵션 동작과 지원 알고리즘이 다르다. 위 실습(특히 `pkeyutl` 의 `-rawin`, `rsa_padding_mode`)은 별도 설치한 OpenSSL 3.x 기준이다.

```bash
openssl version   # LibreSSL 이면 주의
```

**암호 스위트 필터로 "없음"을 증명하려다 틀렸다.** `openssl ciphers -s -tls1_3 -v 'kRSA'` 로 "TLS 1.3 에는 RSA 키 교환이 없다"를 보이려 하면 **안 된다.** TLS 1.3 스위트는 `Kx=any` 라서 그 필터에 그대로 걸려 나오고, 결과가 비어 있지 않은 걸 보면 정반대로 읽게 된다. 올바른 관찰은 **`Kx` 칸 자체가 `any` 로 비어 있다**는 점이다 — 키 교환이 스위트에서 분리됐다는 뜻이다. **필터가 무엇을 걸러내는지 먼저 확인하고 나서 증거로 쓸 것.**

**CA 인증서에 `keyUsage` 확장이 없으면 엉뚱한 오류가 난다.** 자체 CA 를 만들어 실습할 때 확장을 빠뜨리면 Python 에서 `CA cert does not include key usage extension` 이 나온다. 체인 문제로 착각하기 딱 좋은데 **CA 인증서 자체가 규격 미달**이라는 뜻이다. `basicConstraints=CA:TRUE` 와 `keyUsage=keyCertSign,cRLSign` 을 반드시 넣는다.
