# 컨테이너가 조용히 멈췄다 죽는 사건으로 배우는 JVM 메모리와 컨테이너 한도

> 배치 컨테이너가 exit 137로 강제 종료된 운영 장애를 조사하면서 나온 개념들을, 배경 지식 없이도 읽을 수 있게 정리한 학습 문서.
> 계기: 회사 업무 조사 — 대응 기록과 구현 계획은 회사 레포에 있음. 여기서는 일반 개념만 다룬다.
> 작성일: 2026-09-07

---

## 0. 이번 사건 한 문단 요약

예약된 메시지를 한 건씩 보내는 **배치 컨테이너**가, 절반쯤 보낸 상태에서 **22분 동안 아무 진행 없이 멈춰 있다가** 갑자기 죽었다. 종료 코드는 **137**. 그 22분 동안 CPU 사용률은 100% 근처였는데 로그는 한 줄도 늘지 않았다. 애플리케이션 로그 어디에도 `OutOfMemoryError`는 없었다. 컨테이너 메모리 사용량은 한도의 99.8%에 붙어 있었다. 원인은 **JVM의 힙 상한이 컨테이너 메모리 한도보다 크게 잡혀 있었던 것**이었다.

이 문단에 모르는 단어가 하나라도 있으면 이 문서가 도움이 된다. 아래 순서대로 읽으면 위 문단이 전부 이해된다.

---

## 1. 내가 물어본 것들 → 어디를 보면 되나

| 질문 | 섹션 |
|---|---|
| 종료 코드 137이 무슨 뜻인가 | [2장 종료 코드](#2-프로세스가-죽는다는-것--종료-코드부터) |
| 컨테이너에 메모리 한도가 있다는 게 무슨 뜻인가 | [3장 컨테이너 메모리 한도](#3-컨테이너에-메모리-한도가-있다는-것) |
| JVM이 쓰는 메모리는 힙이 전부 아닌가 | [4장 힙 밖의 메모리](#4-jvm이-쓰는-메모리는-힙만이-아니다) |
| 컨테이너 안의 JVM은 자기가 얼마나 쓸 수 있는지 어떻게 아나 | [5장 컨테이너 인식](#5-컨테이너-안의-jvm은-자기-몫을-어떻게-아나) |
| 왜 `OutOfMemoryError`가 안 났는데 메모리 때문에 죽나 | [6장 두 가지 고갈](#6-메모리가-떨어지는-두-가지-방식--이-문서의-핵심) |
| 왜 죽지도 않고 22분이나 멈춰 있었나 | [7장 조용한 정체](#7-왜-조용한-정체가-생기나) |
| 그럼 어떻게 고치나 | [8장 즉시 실패하게 만들기](#8-정체-대신-즉시-실패하게-만드는-법) |
| 같은 JVM 옵션을 두 군데 적어두면 어느 쪽이 이기나 | [9장 옵션 우선순위](#9-같은-옵션을-두-군데서-주면-어느-쪽이-이기나) |
| 컨테이너 크기가 GC 선택에도 영향을 주나 | [10장 GC 선택](#10-컨테이너에서는-gc-종류도-달라진다) |
| 사후 조사를 위해 뭘 남겨둬야 하나 | [11장 남겨야 할 것](#11-사후에-알-수-있으려면-무엇을-남겨야-하나) |
| cgroup이 정확히 뭔가 | [부록 A.1](#a1-cgroup--리눅스가-자원-한도를-거는-방법) |
| 종료 코드 전체 표가 필요하다 | [부록 A.2](#a2-종료-코드-빠른-참조) |
| `MetaspaceSize`와 `MaxMetaspaceSize`는 뭐가 다른가 | [부록 A.3](#a3-metaspace의-두-옵션은-이름만-비슷하고-역할이-다르다) |
| 왜 컨테이너 메모리 사용량이 힙 크기보다 훨씬 큰가 | [부록 A.4](#a4-왜-메모리-사용량rss이-힙보다-큰가) |

---

## 2. 프로세스가 죽는다는 것 — 종료 코드부터

### 2.1 종료 코드란 무엇인가

프로그램이 끝나면 운영체제에 **정수 하나**를 남긴다. 이걸 **종료 코드(exit code)** 또는 종료 상태(exit status)라고 한다. 규칙은 단순하다.

- **0** = 정상 종료
- **0이 아닌 값** = 뭔가 잘못됨

셸에서는 직전 명령의 종료 코드를 `$?`로 볼 수 있다.

```bash
$ ls /존재하지않는경로
ls: /존재하지않는경로: No such file or directory
$ echo $?
1
```

컨테이너 오케스트레이터(ECS, Kubernetes 등)는 컨테이너의 종료 코드를 그대로 기록하고, 알람 조건에 쓴다. **"종료 코드가 0이 아니면 알람"** 같은 규칙이 흔하다.

### 2.2 128 + N — 신호(signal)를 받고 죽었다는 뜻

종료 코드 중 **128보다 큰 값**에는 특별한 의미가 있다. 프로세스가 스스로 끝난 게 아니라 **누군가 보낸 신호(signal)에 의해 강제로 끝났다**는 뜻이고, 다음 규칙을 따른다.

> **종료 코드 = 128 + 신호 번호**

신호(signal)는 운영체제가 프로세스에게 보내는 짧은 통지다. "종료해라", "중단해라" 같은 것들이다. 자주 보는 번호는 이렇다.

| 신호 | 번호 | 종료 코드 | 뜻 |
|---|---|---|---|
| SIGINT | 2 | 130 | 사용자가 Ctrl+C를 눌렀다 |
| SIGABRT | 6 | 134 | 프로세스가 스스로 비정상 중단을 선언했다 |
| SIGKILL | 9 | **137** | **강제 종료. 거부할 수 없다** |
| SIGTERM | 15 | 143 | 정상 종료 요청. 프로세스가 정리할 시간을 갖는다 |

직접 확인해보면 이렇다.

```bash
$ sh -c 'sleep 30 & p=$!; kill -9 $p; wait $p'
sh: line 1: 24409 Killed: 9    sleep 30
$ echo $?
137
```

### 2.3 137이 특별한 이유 — 유언을 남길 수 없다

**SIGKILL(9)은 프로세스가 가로챌 수 없는 유일한 종료 신호다.** SIGTERM은 프로그램이 받아서 "잠깐, 정리 좀 하고요"라고 할 수 있지만, SIGKILL은 커널이 프로세스를 그 자리에서 회수한다.

이게 조사할 때 중요하다.

> **137로 죽은 프로세스는 자기가 왜 죽는지 로그에 남길 기회가 없었다.**

애플리케이션 로그가 문장 중간에 뚝 끊겨 있다면, 그건 버그로 로그가 안 찍힌 게 아니라 **로그를 찍을 틈이 없었다**는 신호다. 그래서 137을 보면 애플리케이션 로그가 아니라 **바깥**(커널, 오케스트레이터, 자원 지표)을 봐야 한다.

---

## 3. 컨테이너에 메모리 한도가 있다는 것

### 3.1 한도는 어디에 걸리나

컨테이너를 띄울 때 보통 이렇게 지정한다.

```
cpu: 512      (0.5 vCPU)
memory: 2048  (2048 MB)
```

이 "memory 2048"은 **권고가 아니라 강제 한도**다. 리눅스 커널의 **cgroup**(control group)이라는 기능이 이 프로세스 그룹이 쓸 수 있는 물리 메모리 총량을 실제로 제한한다. → 자세히는 [부록 A.1](#a1-cgroup--리눅스가-자원-한도를-거는-방법)

### 3.2 한도를 넘으면 무슨 일이 일어나나

프로세스가 한도를 넘겨 메모리를 더 요구하면, 커널은 두 단계로 반응한다.

**1단계 — 회수해서 버틴다.** 당장 안 쓰는 메모리를 먼저 뱉어내게 만든다. 대표적으로 **페이지 캐시**(디스크에서 읽어둔 내용을 메모리에 캐싱해 둔 것)를 비운다. 이건 다시 읽으면 되니까 버려도 되는 메모리다.

**2단계 — 죽인다.** 회수해도 부족하면 커널의 **OOM Killer**(Out Of Memory Killer)가 그룹 안의 프로세스를 골라 **SIGKILL**을 보낸다. 컨테이너는 보통 프로세스가 하나뿐이라, 그게 곧 컨테이너의 죽음이다.

그래서 **컨테이너 메모리 초과 = 종료 코드 137**이 된다.

### 3.3 "컨테이너가 죽는 것"과 "앱이 죽는 것"은 다르다

이 구분이 6장의 전부이므로 미리 못 박아둔다.

| | 누가 판단하나 | 앱이 아나 | 로그 |
|---|---|---|---|
| 앱이 스스로 종료 | 애플리케이션(JVM) | 안다 | 남길 수 있다 |
| 커널이 죽임 | 리눅스 커널 | 모른다 | 못 남긴다 |

---

## 4. JVM이 쓰는 메모리는 힙만이 아니다

이 장을 건너뛰면 6장이 이해되지 않는다.

### 4.1 힙(heap) — 우리가 아는 그 메모리

**힙**은 자바 프로그램이 `new`로 만든 객체가 담기는 공간이다. 가비지 컬렉터(GC)가 관리하는 것도 여기다. `-Xmx`나 `-XX:MaxRAMPercentage`로 상한을 정하는 것도 이것이다.

**대부분의 사람이 "JVM 메모리"라고 하면 이것만 떠올린다. 그게 문제의 시작이다.**

### 4.2 힙 밖에도 메모리가 있다

JVM은 힙 말고도 여러 곳에 메모리를 쓴다. 이걸 통틀어 **네이티브 메모리** 또는 힙 외(off-heap) 메모리라고 부른다.

| 영역 | 무엇이 담기나 | 대략 크기 |
|---|---|---|
| **Metaspace** | 클래스 정보(어떤 필드·메서드가 있는지). 로드한 클래스 수에 비례 | 수십~수백 MB. 프레임워크가 많으면 크다 |
| **Code Cache** | JIT 컴파일러가 자바 바이트코드를 기계어로 번역해 둔 결과 | 수십 MB |
| **스레드 스택** | 스레드마다 하나씩. 메서드 호출 정보 | 스레드당 약 1MB × 스레드 수 |
| **Direct Buffer** | `ByteBuffer.allocateDirect()`. 네트워크 라이브러리가 많이 쓴다 | 라이브러리에 따라 |
| **GC 자체 자료구조** | GC가 힙을 추적하려고 쓰는 메모리 | 힙 크기에 비례 |

### 4.3 그래서 실제 사용량은 항상 힙보다 크다

핵심 결론이다.

```
컨테이너가 실제로 쓰는 메모리
  = 힙 + Metaspace + Code Cache + 스레드 스택 + Direct Buffer + GC 구조 + …
```

컨테이너 한도는 이 **합계**에 걸린다. 힙에만 거는 게 아니다. → [부록 A.4](#a4-왜-메모리-사용량rss이-힙보다-큰가)

---

## 5. 컨테이너 안의 JVM은 자기 몫을 어떻게 아나

### 5.1 옛날에는 몰랐다

컨테이너가 흔해지기 전, JVM은 시작할 때 **호스트 머신 전체의 물리 메모리**를 보고 힙 크기를 정했다. 그래서 64GB짜리 서버에서 메모리 512MB로 제한된 컨테이너를 띄우면, JVM은 "64GB 머신이구나" 하고 힙을 16GB로 잡았다. 당연히 곧바로 죽었다.

### 5.2 지금은 안다 — UseContainerSupport

현대 JVM에는 **컨테이너 자동 감지** 기능이 있다. 공식 문서(JDK 21 `java` 명령 레퍼런스)의 설명은 이렇다.

> **Linux only:** The VM now provides automatic container detection support, which allows the VM to determine the amount of memory and number of processors that are available to a Java process running in docker containers. It uses this information to allocate system resources. The default for this flag is `true`.

정리하면:

- **리눅스 전용** 기능이다 (macOS JVM에는 이 플래그가 아예 없다 — 직접 확인함)
- **기본값이 켜짐**이라 따로 켤 필요가 없다
- JVM이 cgroup 한도를 읽어서 "내가 쓸 수 있는 메모리·CPU"를 판단한다

진단이 필요하면 `-Xlog:os+container=trace`로 JVM이 무엇을 읽었는지 볼 수 있다.

### 5.3 힙 상한을 절대값이 아니라 비율로 주는 이유

컨테이너에서는 `-Xmx512m` 같은 절대값보다 **비율**을 쓰는 게 낫다. 컨테이너 크기를 바꿔도 옵션을 안 고쳐도 되기 때문이다.

| 옵션 | 뜻 |
|---|---|
| `-XX:MaxRAMPercentage=65` | 힙 최대치를 사용 가능 메모리의 65%로 |
| `-XX:InitialRAMPercentage` | 시작할 때 잡는 힙 크기 비율 |
| `-XX:MinRAMPercentage` | (이름과 달리 "최소 힙"이 아니다. 물리 메모리가 아주 작을 때 쓰이는 별도 규칙) |

**기본값은 생각보다 작다.** JDK 21에서 직접 확인한 값이다.

```
MaxRAMPercentage     = 25.0
InitialRAMPercentage = 1.5625
MinRAMPercentage     = 50.0
```

즉 아무 설정도 안 하면 JVM은 **가용 메모리의 25%만** 힙으로 쓴다. 컨테이너를 크게 줬는데 성능이 안 나온다면 이 기본값 때문일 수 있다.

> **확인하는 법**: `java -XX:+PrintFlagsFinal -version | grep RAMPercentage`
> 맨 오른쪽에 `{default}` / `{command line}` / `{environment}`로 **그 값이 어디서 왔는지**까지 알려준다. 9장에서 이걸 쓴다.

---

## 6. 메모리가 떨어지는 두 가지 방식 — 이 문서의 핵심

여기가 사건의 핵심이다. **메모리가 부족해지는 곳이 두 군데이고, 둘은 완전히 다르게 나타난다.**

### 6.1 안에서 터진다 — 힙 부족

힙이 꽉 차서 GC를 돌려도 공간을 못 만들면, **JVM이 스스로 알아채고** `OutOfMemoryError`를 던진다.

```
Exception in thread "main" java.lang.OutOfMemoryError: Java heap space
	at com.example.Something.doWork(Something.java:42)
```

특징:

- **JVM이 인지한다** → 스택 트레이스를 남길 수 있다
- 기본 동작은 그 스레드만 죽는 것이라, 프로그램이 비틀거리며 계속 살아 있을 수도 있다
- 종료된다면 종료 코드는 보통 **1**(직접 확인)

### 6.2 밖에서 죽는다 — 컨테이너 한도 초과

힙 + 네이티브 메모리의 **합계**가 컨테이너 한도를 넘으면, 커널이 SIGKILL을 보낸다.

특징:

- **JVM은 자기가 위험한 줄 모른다** → 마지막 순간까지 "정상"이라고 믿는다
- `OutOfMemoryError`는 **나지 않는다**
- 로그가 문장 중간에 끊긴다
- 종료 코드 **137**

### 6.3 둘의 신호가 완전히 다르다

| | 힙 부족 (6.1) | 컨테이너 한도 초과 (6.2) |
|---|---|---|
| 누가 감지 | JVM | 리눅스 커널 |
| `OutOfMemoryError` | **난다** | **안 난다** |
| 애플리케이션 로그 | 스택 트레이스 있음 | 갑자기 끊김 |
| 종료 코드 | 1 (기본) / 3 (8.2절 플래그 사용 시) | **137** |
| 오케스트레이터가 남기는 사유 | 없음(정상 종료 경로) | "메모리 때문에 죽였다" 류의 메시지 |
| 조사 시작점 | 앱 로그 | 자원 지표 · 커널/오케스트레이터 이벤트 |

**0장 사건은 6.2였다.** 그래서 앱 로그를 아무리 뒤져도 `OutOfMemoryError`가 없었던 것이다.

---

## 7. 왜 "조용한 정체"가 생기나

6.2는 이해했다 치자. 그런데 **왜 곧바로 안 죽고 22분이나 멈춰 있었을까?** 여기에 두 가지가 겹친다.

### 7.1 힙 상한이 컨테이너 여유보다 크게 잡혀 있었다

사건 당시 설정을 일반화하면 이랬다.

```
컨테이너 한도          512MB
├─ 힙 상한   512 × 75% = 384MB   ← 아직 여유 있다고 JVM은 판단
└─ Metaspace 상한       256MB
   ────────────────────────────
   힙 + Metaspace만 해도 640MB > 512MB
```

**힙 상한(384MB)과 Metaspace 상한(256MB)의 합이 컨테이너 한도(512MB)보다 크다.** 이러면 이런 일이 생긴다.

> 힙은 아직 안 찼다 → **JVM은 `OutOfMemoryError`를 던질 이유가 없다**
> 그런데 힙 + 네이티브 합계는 이미 한도다 → **커널은 계속 메모리를 회수한다**

즉 **JVM은 "정상"이라 믿고, 커널은 "한계"라고 판단하는 상태**가 만들어진다. 아무도 멈추라고 말하지 않으니 프로그램은 계속 돌려고 한다.

### 7.2 GC 스래싱 — 일은 안 하고 청소만 한다

힙이 가득 차기 직전이 되면 GC는 점점 자주, 점점 오래 돈다. 겨우 조금 비우고 나면 금방 다시 차니까 또 돈다. 결국 **CPU 시간의 대부분을 GC가 쓰고 애플리케이션 코드는 거의 실행되지 않는** 상태가 된다. 이걸 **GC 스래싱(thrashing)**이라고 한다.

바깥에서 보면 이렇게 보인다.

- CPU 사용률: **100% 근처** (열심히 일하는 것처럼 보인다)
- 실제 진행: **거의 0** (로그가 안 늘어난다)

### 7.3 페이지 캐시 회수 — 프로그램 코드가 디스크로 밀려난다

3.2절의 1단계가 여기서 악화 요인이 된다. 커널은 한도를 지키려고 페이지 캐시를 버리는데, **실행 중인 프로그램의 코드 페이지도 그 대상**이다. 그러면 다음에 그 코드를 실행할 때 디스크에서 다시 읽어야 한다.

이게 반복되면 "메모리는 꽉 찼고 CPU는 바쁜데 진행은 안 되는" 상태가 길게 이어진다.

### 7.4 정체가 왜 위험한가 — 알람이 늦다

죽으면 알람이 온다. 그런데 **멈춰 있으면 아무 알람도 안 온다.** 프로세스는 살아 있고, 헬스체크가 있다면 통과할 수도 있다. 사건에서 22분을 아무도 모른 이유가 이것이다.

> **교훈: 애매하게 살아 있는 것보다 확실하게 죽는 게 낫다.**

---

## 8. 정체 대신 즉시 실패하게 만드는 법

### 8.1 힙 상한을 컨테이너보다 확실히 작게 잡는다

7.1의 반대로 하면 된다. **힙이 컨테이너보다 먼저 차게** 만들면, JVM이 `OutOfMemoryError`를 던진다 — 즉 6.2(밖에서 죽음)를 6.1(안에서 터짐)로 바꾸는 것이다.

계산은 뺄셈이다.

```
컨테이너 한도                       2048MB
− Metaspace 상한                    −256MB
− Code Cache · 스레드 스택 · 네이티브  −약 150MB
− 안전 여유                          −약 200MB
  ─────────────────────────────────────────
  힙에 줘도 되는 최대               약 1440MB  → 약 70%
```

그래서 **`MaxRAMPercentage`를 65~70% 근처**로 두는 조언이 나온다. 75% 이상이면 Metaspace 상한이 큰 애플리케이션에서 7.1 상황이 만들어질 수 있다.

> 주의: Metaspace 상한을 **설정하지 않으면 사실상 무제한**이다. 그러면 위 뺄셈이 성립하지 않는다. → [부록 A.3](#a3-metaspace의-두-옵션은-이름만-비슷하고-역할이-다르다)

### 8.2 OOM이 나면 즉시 종료시킨다

`OutOfMemoryError`가 나도 JVM은 기본적으로 **계속 살아 있으려 한다**. 에러가 난 스레드만 죽고 나머지는 돈다. 배치처럼 "실패했으면 그냥 죽고 다시 실행하는 게 나은" 프로그램에서는 이게 오히려 해롭다.

이럴 때 쓰는 플래그가 두 개 있다. JDK 21로 직접 측정한 결과다.

| 플래그 | 동작 | 종료 코드 |
|---|---|---|
| (없음) | 에러를 던지고 계속 시도 | 1 (결국 종료될 경우) |
| `-XX:+ExitOnOutOfMemoryError` | 첫 OOM에 즉시 종료 | **3** |
| `-XX:+CrashOnOutOfMemoryError` | 즉시 중단 + 크래시 로그 생성 | **134** (= 128 + SIGABRT 6) |

`ExitOnOutOfMemoryError`를 켰을 때 stdout에 남는 문장은 이렇다.

```
Terminating due to java.lang.OutOfMemoryError: Java heap space
```

> **직접 확인한 값이다.** 종료 코드 3은 JDK의 `java` 명령 공식 레퍼런스에 명시돼 있지 않아, 버전이 바뀌면 달라질 수 있다고 보는 편이 안전하다. 운영에 반영하기 전 자기 JDK 버전에서 한 번 재보는 것을 권한다.

### 8.3 그런데 이걸 켜면 "OOM의 얼굴"이 바뀐다

이게 실무에서 놓치기 쉬운 부분이다. 8.1 + 8.2를 적용하면 **같은 OOM인데 남는 흔적이 완전히 달라진다.**

| | 적용 전 | 적용 후 (힙 OOM) |
|---|---|---|
| 죽인 주체 | 커널 | JVM 자신 |
| 종료 코드 | **137** | **3** |
| 오케스트레이터가 남기는 사유 | "메모리 때문에 죽였다" | **없음** (정상 종료 경로라서) |
| 애플리케이션 로그 | 없음 | `Terminating due to java.lang.OutOfMemoryError…` |

즉 팀이 학습해 온 **"137이면 메모리 문제"라는 판단 기준이 더 이상 맞지 않는다.** 앞으로는 이렇게 나뉜다.

- **3** → 힙 부족. 앱 로그에 원인이 있다
- **137** → 힙 밖(네이티브 메모리)의 문제이거나 다른 이유. 커널·오케스트레이터 쪽을 봐야 한다

**알람 규칙도 같이 점검해야 한다.** "종료 코드가 137이면 알람" 같은 조건이면 3으로 바뀐 순간 알람이 조용해진다. **"0이 아니면 알람"**처럼 열려 있어야 안전하다.

---

## 9. 같은 옵션을 두 군데서 주면 어느 쪽이 이기나

### 9.1 JVM 옵션은 여러 경로로 들어온다

컨테이너 환경에서는 JVM 옵션을 주는 자리가 최소 두 곳이다.

1. **이미지의 실행 명령**(Dockerfile의 `ENTRYPOINT`/`CMD`) — 커맨드라인 인자
2. **환경변수 `JAVA_TOOL_OPTIONS`** — 배포 설정(태스크 정의, 매니페스트 등)에서 주입

`JAVA_TOOL_OPTIONS`는 JVMTI(JVM Tool Interface) 명세에 정의된 표준 환경변수다. 명세의 설명은 이렇다.

> "Since the command-line cannot always be accessed or modified, for example in embedded VMs or simply VMs launched deep within scripts, a `JAVA_TOOL_OPTIONS` variable is provided…"

즉 **커맨드라인을 못 건드리는 상황을 위한 우회로**다.

### 9.2 순서 — 환경변수가 "앞에" 붙는다

같은 명세에 순서가 명시돼 있다.

> "`JNI_CreateJavaVM` will **prepend these options** to the options supplied in its `JavaVMInitArgs` argument."

**prepend = 앞에 붙인다.** 그러니까 실제로 JVM이 받는 옵션 목록은 이렇게 된다.

```
[ JAVA_TOOL_OPTIONS의 옵션들 ]  [ 커맨드라인 옵션들 ]
         먼저                         나중
```

그리고 같은 `-XX` 옵션이 두 번 나오면 **나중에 온 것이 이긴다.** 두 사실을 합치면 결론은 하나다.

> **커맨드라인이 `JAVA_TOOL_OPTIONS`를 이긴다.**

직접 확인해보면 이렇다.

```bash
$ JAVA_TOOL_OPTIONS="-XX:MaxRAMPercentage=60" \
  java -XX:MaxRAMPercentage=75 -XX:+PrintFlagsFinal -version | grep MaxRAMPercentage
Picked up JAVA_TOOL_OPTIONS: -XX:MaxRAMPercentage=60
   double MaxRAMPercentage = 75.000000  {product} {command line}
```

환경변수로 60을 줬는데 **최종값은 75**이고, 출처가 `{command line}`이라고 찍혀 있다.

### 9.3 왜 이게 실전에서 함정인가

배포 설정(환경변수)은 이미지 재빌드 없이 고칠 수 있으니, 메모리 문제가 생기면 **거기부터 고치고 싶어진다.** 그런데 Dockerfile에 같은 옵션이 이미 있으면 **환경변수를 아무리 고쳐도 아무 일도 일어나지 않는다.**

더 나쁜 건 조용하다는 것이다. 에러도 경고도 없고, 설정 파일에는 "60%로 제한해 뒀다"고 적혀 있다. 다음 사람이 그걸 읽고 사실이라고 믿는다.

**대응 원칙 두 가지.**

1. **같은 옵션을 두 군데 두지 않는다.** 한쪽만 남긴다.
2. **실제 적용값을 출처와 함께 확인한다** — `-XX:+PrintFlagsFinal`의 `{command line}` / `{environment}` / `{default}` 표시를 믿는다. 설정 파일을 믿지 않는다.

---

## 10. 컨테이너에서는 GC 종류도 달라진다

### 10.1 JVM은 머신 크기를 보고 GC를 고른다

JVM은 아무것도 지정하지 않으면 **환경에 맞춰 GC를 자동 선택**한다(이걸 ergonomics라고 부른다). 판단 재료는 **사용 가능한 CPU 개수와 메모리 크기**다.

컨테이너에서는 5.2절의 컨테이너 감지가 이 값을 결정한다. 그러니 **컨테이너 크기를 바꾸면 GC가 바뀔 수 있다.**

### 10.2 직접 측정한 결과

CPU 개수와 메모리를 바꿔가며 어떤 GC가 선택되는지 재봤다.

| CPU | 메모리 | 선택된 GC |
|---|---|---|
| 1 | 1024MB ~ 4096MB | **SerialGC** |
| 2 이상 | 1024MB ~ 4096MB | **G1GC** |

이 측정에서는 **CPU 개수가 결정적**이었다. CPU가 1개로 인식되면 메모리를 아무리 늘려도 SerialGC였다.

> **측정 조건을 밝혀둔다.** macOS JVM에서 `-XX:ActiveProcessorCount`와 `-XX:MaxRAM`으로 흉내 낸 값이다. macOS에는 컨테이너 감지 기능 자체가 없으므로(5.2절), 실제 리눅스 컨테이너와 다를 수 있다. **자기 환경의 값은 GC 로그로 직접 확인해야 한다.**

### 10.3 왜 신경 써야 하나

GC가 바뀌면 딸려오는 옵션의 의미도 바뀐다. 예를 들어 `-XX:MaxGCPauseMillis`는 G1GC의 목표 지연 시간을 지정하는 옵션인데, **SerialGC에서는 의미가 없다.** 설정 파일에는 남아 있지만 아무 효과가 없는 상태가 된다 — 9.3절과 같은 종류의 함정이다.

0.5 vCPU 같은 **1 미만의 CPU 할당**은 특히 조심할 만하다. JVM이 CPU를 몇 개로 인식하는지에 따라 GC가 갈린다.

**확인법**: GC 로그를 켜면 첫 줄 근처에 어떤 GC가 쓰이는지 나온다.

```
-Xlog:gc*:stdout
```

---

## 11. 사후에 알 수 있으려면 무엇을 남겨야 하나

0장 사건에서 조사가 어려웠던 이유는 **증거가 없어서**였다. 무엇을 미리 켜뒀어야 했는지 정리한다.

### 11.1 GC 로그 — 켜두지 않으면 사후에 못 만든다

`-Xlog:gc*:stdout`을 켜두면 GC가 언제 얼마나 돌았는지가 표준 출력으로 나가고, 로그 수집기가 알아서 가져간다. 7.2절의 GC 스래싱은 **GC 로그가 없으면 사실상 증명할 수 없다.** 배치처럼 실행이 짧은 프로그램은 로그량 부담도 작다.

> JSON 구조화 로그를 쓰는 프로젝트라면 GC 로그가 평문으로 섞인다는 점을 미리 알아두면 좋다. 보통 JVM 시작 배너 같은 평문이 이미 섞여 있어 큰 문제는 아니다.

### 11.2 종료 코드 — 알람 조건을 열어둔다

8.3절에서 봤듯 종료 코드는 설정 변경으로 바뀔 수 있다. 알람 조건은 **"특정 코드"가 아니라 "0이 아닌 모든 코드"**로 두는 편이 안전하다.

### 11.3 오케스트레이터 이벤트는 오래 보관되지 않는다

컨테이너 오케스트레이터가 기록하는 "왜 죽었는지"는 **보관 기간이 짧다.** 예를 들어 ECS는 중지된 태스크 정보를 약 1시간만 유지한다. 사건이 새벽에 나고 아침에 조사를 시작하면 **이미 사라진 뒤다.**

대응은 단순하다. **종료 이벤트를 받아서 로그로 남겨두는 것.** 대부분의 클라우드에 이벤트를 로그 저장소로 흘려보내는 기능이 있다. 이건 사건이 나기 **전에** 해둬야 하는 일이다.

### 11.4 자원 사용률 알람 — 다만 무엇을 감시하는지 알고 걸어야 한다

"메모리 사용률 85% 넘으면 경고" 같은 알람은 유용하다. 그런데 8.1절을 적용하고 나면 **의미가 달라진다.**

힙 상한을 컨테이너의 65%로 잡아두면, 힙이 꽉 차서 죽는 순간에도 **컨테이너 전체 사용률은 85%에 못 미칠 수 있다.** 힙 OOM은 이 알람이 잡는 게 아니라 종료 코드가 잡는다.

그러면 이 알람은 쓸모없나? 아니다. **힙 상한이 잡지 못하는 영역** — Metaspace가 계속 늘거나, Direct Buffer가 새거나, 스레드가 무한히 늘어나는 경우 — 를 감시한다. 목적을 이렇게 이해하고 걸어야 한다.

---

## 12. 전체 정리 — 사건을 한 줄씩 되짚기

0장 문단을 이제 다시 읽으면 이렇게 해석된다.

| 사건의 문장 | 무슨 일이었나 |
|---|---|
| "종료 코드 137" | 커널이 SIGKILL로 강제 종료했다. 앱은 유언을 못 남겼다 (2.3) |
| "`OutOfMemoryError`가 없었다" | 힙은 안 찼다. 부족했던 건 **힙 + 네이티브의 합계** (6.2) |
| "22분간 멈춰 있었다" | 힙 상한이 컨테이너보다 커서 JVM이 위험을 인지하지 못했다. GC 스래싱 + 페이지 회수 (7.1~7.3) |
| "CPU는 100%인데 로그가 안 늘었다" | GC만 돌고 애플리케이션 코드는 실행되지 않았다 (7.2) |
| "아무도 22분간 몰랐다" | 죽어야 알람이 오는데 살아 있었다 (7.4) |

**재발을 막는 설정 세 줄**로 요약하면 이렇다.

```
-XX:MaxRAMPercentage=65        # 힙이 컨테이너보다 먼저 차게 (8.1)
-XX:+ExitOnOutOfMemoryError    # 차면 버티지 말고 즉시 종료 (8.2)
-Xlog:gc*:stdout               # 무슨 일이 있었는지 남기기 (11.1)
```

그리고 **설정과 함께 바뀌는 것들**을 잊지 않는다.

- 종료 코드가 137에서 3으로 바뀐다 → 알람 조건 점검 (8.3)
- 같은 옵션이 다른 곳에도 있으면 이 세 줄이 무시될 수 있다 → 실제 적용값 확인 (9.3)
- 컨테이너 크기가 바뀌면 GC도 바뀔 수 있다 → GC 로그로 확인 (10.3)

---

# 부록

## A.1 cgroup — 리눅스가 자원 한도를 거는 방법

**cgroup**(control group)은 리눅스 커널 기능으로, 프로세스들을 그룹으로 묶어 그 그룹이 쓸 수 있는 CPU·메모리·디스크 I/O 총량을 제한한다. 컨테이너는 사실 **"cgroup으로 자원이 제한되고 namespace로 시야가 격리된 평범한 프로세스"**에 가깝다. 가상 머신처럼 별도의 운영체제가 도는 게 아니다.

메모리 한도는 파일시스템처럼 노출된다. 컨테이너 안에서 읽을 수 있다.

```bash
# cgroup v2
$ cat /sys/fs/cgroup/memory.max        # 한도
$ cat /sys/fs/cgroup/memory.current    # 현재 사용량
```

5.2절의 컨테이너 감지는 JVM이 바로 이 값을 읽는 것이다. "JVM이 컨테이너를 안다"는 건 마법이 아니라 **이 파일을 읽는다**는 뜻이다.

## A.2 종료 코드 빠른 참조

| 코드 | 의미 | 조사 시작점 |
|---|---|---|
| 0 | 정상 | — |
| 1 | 일반적인 실패. 자바에서는 처리되지 않은 예외 포함 | 애플리케이션 로그 |
| **3** | `-XX:+ExitOnOutOfMemoryError` 사용 시 OOM (측정값, 8.2) | 애플리케이션 stdout |
| 125~127 | 컨테이너 런타임이 실행 자체를 못 함 (이미지·엔트리포인트 문제) | 이미지 설정 |
| 130 | SIGINT (Ctrl+C) | 사용자 조작 |
| **134** | SIGABRT — `CrashOnOutOfMemoryError` 포함 | 크래시 로그(`hs_err_pid*.log`) |
| **137** | SIGKILL — **메모리 한도 초과가 가장 흔함** | 자원 지표 · 오케스트레이터 이벤트 |
| 143 | SIGTERM — 정상 종료 요청(배포·스케일다운 등) | 보통 정상 |

> 137이 항상 메모리는 아니다. 운영자가 강제 종료했거나 헬스체크 실패로 오케스트레이터가 죽였을 수도 있다. **자원 지표를 함께 봐야 판정된다.**

## A.3 Metaspace의 두 옵션은 이름만 비슷하고 역할이 다르다

혼동하기 쉬운 한 쌍이다.

| 옵션 | 역할 |
|---|---|
| `-XX:MetaspaceSize` | **첫 GC를 트리거하는 임계값.** "여기까지 차면 클래스 정리를 한번 해봐라"는 신호. 상한이 아니다 |
| `-XX:MaxMetaspaceSize` | **진짜 상한.** 이걸 넘으면 `OutOfMemoryError: Metaspace` |

주의할 점 두 가지.

1. **`MaxMetaspaceSize`를 지정하지 않으면 사실상 무제한**이다. 그러면 8.1절의 뺄셈이 성립하지 않아 컨테이너 한도를 넘길 수 있다.
2. `MetaspaceSize`를 크게 잡으면(예: 256MB) 그만큼 찰 때까지 정리를 안 하므로, **실제 커밋되는 메타스페이스가 커진다.** 상한과 같은 값으로 두면 사실상 "상한까지 그냥 쓴다"는 뜻이 된다.

## A.4 왜 메모리 사용량(RSS)이 힙보다 큰가

**RSS**(Resident Set Size)는 프로세스가 실제로 물리 메모리에 올려둔 양이다. 컨테이너 한도가 걸리는 대상이고, 모니터링 도구가 "메모리 사용량"으로 보여주는 것도 대개 이 값이다.

RSS가 힙보다 큰 이유는 4.2절의 목록 그대로다. 대략적인 구성은 이렇다.

```
RSS ≈ 힙(실제 사용분)
    + Metaspace
    + Code Cache
    + 스레드 스택 × 스레드 수
    + Direct Buffer
    + GC 자료구조
    + JVM 자체 코드와 라이브러리
```

그래서 **"힙 512MB니까 컨테이너 512MB면 되겠지"는 항상 틀린다.** 최소 몇백 MB의 여유가 더 필요하다.

정확히 알고 싶으면 **NMT**(Native Memory Tracking)를 쓴다.

```bash
# 실행 시
java -XX:NativeMemoryTracking=summary ...

# 실행 중 조회
jcmd <pid> VM.native_memory summary
```

영역별로 예약(reserved)·커밋(committed) 크기를 나눠 보여주므로, 어디가 부풀었는지 짚을 수 있다.

---

## 참고

- JDK 21 `java` 명령 레퍼런스 — `UseContainerSupport`, 진단 로깅 옵션
- JVMTI 명세 — `JAVA_TOOL_OPTIONS`의 목적과 prepend 순서
- 본문의 종료 코드·기본값·GC 선택 수치는 **JDK 21.0.5(Temurin)에서 직접 측정**한 값이다. 버전·플랫폼에 따라 달라질 수 있으므로, 운영에 반영하기 전 자기 환경에서 재확인하는 것을 권한다.
